#!/usr/bin/env bash
# Placeholder (0.T4) — the real restore (archive verification, explicit
# confirmation, refusal while the stack is up) arrives with MVP 1.T3 (INF-16).
echo "restore.sh: not implemented yet — arrives with MVP 1.T3 (phase 1)" >&2
exit 1
