#!/usr/bin/env bash
# Placeholder (0.T4) — the real backup (dump + .env + optional config.yaml
# into a dated archive under /backups) arrives with MVP 1.T2 (INF-16).
echo "backup.sh: not implemented yet — arrives with MVP 1.T2 (phase 1)" >&2
exit 1
