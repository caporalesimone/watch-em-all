// The plugin's i18n namespace. The core registers these dictionaries (it owns
// svelte-i18n); the plugin only ships data. en is the fallback. Default export:
// { <locale>: messages }.
import en from './en.json';

export default { en };
