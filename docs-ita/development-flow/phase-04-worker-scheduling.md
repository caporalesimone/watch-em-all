# Fase 4 — Worker e scheduling

> Stato: 🚧 in corso — avviata 2026-06-25, versione di lavoro `0.4.x` · Prerequisiti: Fase 3 ✅ · [Indice del flusso](README.md)

## Obiettivo

L'automazione: il container worker (sostituisce lo stub di fase 0), gli slot per-scraper (1..N al giorno, indipendenti), il runner seriale con lock e timeout, la cache di scrape con la sua configurazione admin, i record di run e i log osservabili dall'admin.

## Risultato apprezzabile

Imposti "Dragon Store alle 14:30" e alle 14:30 lo scrape parte da solo; nella pagina admin vedi la riga di log, la run con i contatori e il battito del worker. Spegni il worker per un'ora a cavallo dello slot: al riavvio recupera.

## MVP

### Backend

- [x] **4.B0 — Guardia di disallineamento schema DB (dev)** ✅ (0.4.0): all'avvio confronta il modello ORM (`Base.metadata`) con lo schema reale del DB (`sqlalchemy.inspect`) e segnala **tabelle/colonne mancanti** — `log.warning` per voce + salvataggio su `app.state.schema_drift`. Il feed **admin-only** `GET /api/admin/errors` (errori/warning per l'admin, `{source,type,title,description}`) include lo schema-drift come **una voce**, **solo a flag attivo** (mai sulla sonda pubblica `/api/health`, mai a utenti comuni/anonimi: è un'informazione che riguarda l'admin). Il flag `WEA_SCHEMA_DRIFT_ALERT` ha **default OFF nel codice**, ma è presente e `true` in `.env`/`.env.example` (`=false` per spegnerlo). Copre **sia le tabelle core (`Base.metadata`) sia quelle dei plugin**: il drift può stare anche su una tabella di plugin (con `MetaData` propria), quindi serve una convenzione (**DECISA 2026-06-25**): ogni `BasePlugin` espone un attributo **`table_metadata: MetaData | None`** (la `_Base.metadata` del plugin; `None` per i plugin senza tabelle, es. i notifier), raccolto dal registry in `LoadedPlugin`; il check itera `Base.metadata` + le `table_metadata` dei plugin caricati. **Obbligatorio, enforced al load**: un plugin che possiede tabelle `plugin_<id>_*` ma **non** espone `table_metadata` è **rifiutato dal registry** (`PluginLoadError`) — un plugin reale rotto non parte affatto, invece di sfuggire in silenzio al drift; `table_metadata` diventa la **fonte unica** dello schema del plugin (guida il drift-check e, in prospettiva, il `create_all`). Aggiunge una **nuova regola** nelle developer-rules vicino a CTX-R6/DB-R. Il meccanismo esatto di rilevamento dell'omissione (create_all core-driven dalla `table_metadata` vs check post-`initialize` sui nomi `plugin_<id>_*`) si fissa nell'analisi di dettaglio di 4.B0. Non sostituisce le migrazioni (Alembic non c'è ancora): in dev, per applicare un cambio di schema, resta il reset del volume. **Casi reali che l'hanno motivata (fase 3)**: (1) colonne `brand`/`tags` aggiunte alla tabella core `products` → `GET /api/catalog` 500; (2) colonna `name` aggiunta alla tabella **di plugin** `plugin_dragon_store_watches` → `GET /watches` 500 (proprio questo conferma che il check NON può limitarsi alle tabelle core). *Verifica: aggiungo una colonna a un modello (core o plugin) senza ricreare il DB → warning nei log + flag attivo, da admin) `GET /api/admin/errors` contiene la voce schema-drift; flag `false` o utente non-admin/anonimo → vuoto/403/401.*
- [x] **4.B0a — Versione del plugin esposta (priorità early)** ✅ (0.4.0): il **registry** legge `version` dal `manifest.json` di ogni plugin e la espone in `PluginInfo` / `GET /api/plugins` (oggi riporta solo name/type/route_base/icon/display_name; il registry **non** legge `version`). L'admin la **vede** accanto a nome/tipo/stato `enabled` — sulla pagina admin dello scraper (vicino a 4.F2) e/o in un elenco plugin; la pagina admin **dedicata** ai plugin arriva più avanti. *Verifica: `GET /api/plugins` riporta `version` per ogni plugin (es. dragon_store `0.2.0`) e la versione compare nella UI admin.*
- [x] **4.B0b — Swap Adminer → pgweb (browser DB di sviluppo)** ✅ (0.4.0): `adminer` sostituito da **`sosedoff/pgweb:0.16.2`** (pinnato, INF-1) **solo in `compose-dev.yml`** e **rimosso del tutto dal `compose.yml` di release** (il deploy kit resta production-only — in emergenza `docker compose exec db psql` sul container `db` sempre acceso, o i dump/restore del container `ops`). pgweb è **sempre acceso, SENZA profilo**: in dev il browser DB lo si vuole sempre, quindi sale con un semplice `up`. **Connection URL** dai `POSTGRES_*` via `DATABASE_URL` (`postgres://…@db:5432/…?sslmode=disable`) → apre **diretto** su `watchemall`, **senza form né login** (niente `--auth-*`: la protezione è la sola presenza nel file di dev + 8081 mai esposto in prod), porta `8081`. **Decisione 2026-06-25 (supera la rename `dev`→`tools`)**: niente profilo `dev`/`tools` in dev; l'unico profilo rimasto è `ops` (container run-once, strutturalmente fuori dall'`up`). **INF-3 riscritta**: il gate dei tool di debug passa dal profilo alla **separazione dei file** (`compose-dev.yml` vs `compose.yml`). Sweep doc completo: README, dev-container (ITA+EN), deployment (ITA+EN), `2-architecture/system-overview`, schema (DB-R6), INF-1 pin, checklist-and-testing, `docs/updates/phase-02|03`, phase-00/01. *Verifica fatta: YAML valido (`pgweb` in dev, nessun browser DB in release); grep `adminer`/`profile dev` lascia solo la voce storica del CHANGELOG + un commento esplicativo. DoD live (pgweb apre diretto su `watchemall`) resta da provare in devcontainer.*
- [x] **4.B1 — Worker reale + heartbeat** ✅ (0.4.0): modulo **`src/worker/`** (sostituisce lo stub 0.T4), avviato con `python -m src.worker`. Boot come il web (engine, schema, plugin, + log dello schema-drift 4.B0), loop al minuto che scrive l'heartbeat (healthcheck compose) e chiama **`tick(now)`** — puro e iniettabile, è il **seam** dove si innesta lo scheduling (4.B2+). Intervallo da `WEA_TICK_SECONDS`, **overridabile a runtime** via il feature flag DB (4.B1a). ([cron-worker](../4-capabilities/core/cron-worker.md), [deployment](../infrastructure/deployment.md)). *Verifica: heartbeat avanza; kill del worker → container unhealthy.*
- [x] **4.B1a — Feature flags dev (DB)** ✅ (0.4.0): tabella core `feature_flags` (key→JSON) **condivisa tra web e worker** (processi separati). API admin via Swagger: `GET /api/admin/feature-flags` (valori effettivi = default + override) e `PATCH` (setta uno o più; solo chiavi note, `KNOWN_FLAGS`). **Azzerata all'avvio del web** → non persistente (torna ai default). Flag noti: **`worker_tick.seconds`** — override runtime dell'intervallo del worker (default `WEA_TICK_SECONDS`), per testare gli step seguenti senza aspettare il minuto reale; **`scrape_now_cooldown.seconds`** — override del cooldown dello **scrape-now manuale** (default 3600 = la costante `SCRAPE_NOW_COOLDOWN_SECONDS`), per provare il countdown UI (es. `30`) senza aspettare l'ora — letto dal router scrape-now sia in `claim_scrape` (POST) sia in `cooldown_status` (GET), così al reload il countdown riflette l'intervallo nuovo. Admin-only, dev-oriented. *Verifica: `PATCH worker_tick.seconds=5` → il worker ticka ogni 5s; `PATCH scrape_now_cooldown.seconds=30` → il GET scrape-now riporta `interval_seconds=30`; restart del web → torna ai default; utente non-admin/anonimo → 403/401.*
- [x] **4.B2 — Tabella schedule + API** ✅ (0.4.0): modello `scraper_schedule` (scraper_id PK, `times` JSON `["HH:MM"]`, enabled, last_slot) + servizio `src/core/schedule.py` (validazione/dedup/sort dei `times` — accetta `HH:MM` o `HH:MM:SS`, CRUD, `set_last_slot`). Router admin `admin_scrapers.py`: `GET /api/admin/scrapers` (scraper schedulabili = quelli che implementano `run_for_user`, col loro schedule) e `PUT /api/admin/scrapers/{id}` (`{times, enabled}`; 422 orario non valido, 404 scraper ignoto) ([scheduling-models](../4-capabilities/contracts/scheduling-models.md)). *Verifica: slot salvati e riletti da Swagger; dedup/sort applicati.*
- [x] **4.B3 — latest_due_slot + catch-up** ✅ (0.4.0): in `src/core/schedule.py`, `latest_due_slot(times, now, tz)` (puro) = slot più recente già passato (oggi o ieri → recupero cross-midnight, solo l'ultimo, mai replay) restituito in **UTC**; `due_slot(schedule, now, tz)` = dovuto se enabled e più recente di `last_slot`. **TZ-aware**: orari interpretati nel fuso `TZ` (`install_tz`, default Europe/Rome), timestamp UTC (BE-13). *Verifica: unit test tabellari (`test_schedule.py`) su slot multipli, recupero, mezzanotte, enabled/last_slot.*
- [x] **4.B4 — Runner seriale** ✅ (0.4.0): `src/worker/runner.py` (`Runner`: coda FIFO + **un solo thread**, dedup di uno scraper già in coda/esecuzione, SCHED-R6); il `tick` del worker fa `dispatch_due` (calcola i dovuti e `submit` al runner, senza bloccare, CRON-R5). Il job (`scraper_job`/`_run_scraper`): un context per run → `configured_users(ctx)` (nuovo sul contratto; Dragon Store = utenti con ≥1 watch) → per utente `stamp_cooldown` (write-only, SCR-R15) + `run_for_user`; errore su un utente = prosegue (POOL-R5); `last_slot` scritto **anche su errore** (CRON-R6) ([scraper-pool](../4-capabilities/core/scraper-pool.md)). *Verifica: `test_runner` (seriale + dedup), `test_worker` (dispatch dei soli dovuti, iterazione utenti + cooldown + last_slot); live: due scraper dovuti → in sequenza, manuale in cooldown dopo la run.*
- [x] **4.B5 — Advisory lock + timeout** ✅ (0.4.0): `src/core/locks.py` — `acquire_scraper_lock(engine, plugin_id)` / `scraper_lock(...)`: lock per-scraper **non bloccante**, su Postgres `pg_try_advisory_lock` (chiave sha256→int64, **cross-container**), fallback process-local (`threading.Lock`) su SQLite/test. **Condiviso** worker↔web: il runner skippa con warning se occupato; lo **scrape-now manuale → 409** (`scrape_in_progress`), tenendo il lock dalla richiesta fino al task in background. **Timeout** da `system_settings` (`scraper_run_timeout_min`, default 30; nuova tabella + `src/core/settings.py`), enforcement **cooperativo** (controllo tra utenti; niente kill del thread). *Verifica: `test_locks` (esclusività+riuso), `test_scrape_lock` (409 con lock tenuto), `test_settings` (default), `test_worker` (stop al deadline).*
- [x] **4.B6 — Record di run** ✅ (0.4.0): modelli `scrape_run` + `scrape_user_log`; il runner apre la run, per ogni utente una riga `scrape_user_log` con contatori da `DeltaCounters` (`found`/`new`/`price_changes`/`removed`) + `http_requests` (delta di `request_count` del client per utente), chiude con stato aggregato (`ok`/`partial`/`error`/`timeout`) e somme. `cache_hits` = 0 fino a 4.B8 (cache); `products_excluded` = 0 finché un plugin non li dichiara. *Verifica: `test_worker` — run con 2 utenti → 1 `scrape_run` (status ok, users_processed 2) + 2 `scrape_user_log`; deadline → run `timeout` con 0 righe utente.*
- [x] **4.B7 — Log di sistema** ✅ (0.4.0): modello `system_log` (id incrementale = cursore, created_at, level info|warning|error, source, message, context_json) + **handler di logging** (`SystemLogHandler` in `src/core/system_log.py`) agganciato al logger `wea` (installato in worker e web): persiste **solo** i record `wea.worker.*` (source `worker`) e `wea.plugin.*` (source `scraper`), il resto resta su stdout (LOG-R1/R4). **Niente riga heartbeat** (decisione 2026-06-26: la liveness resta su `/api/health` + file heartbeat). `GET /api/admin/logs` (admin-only): **due modalità** — `since` assente → ultime `limit` righe, `since=<id>` → righe con `id > since` (LOG-R3); filtri `level`/`source`; `limit` 1–1000 (default 200). **Retention** (MNT-R2): `src/core/maintenance.py` `purge_expired` — il worker, **una volta al giorno** (boot + cambio data), cancella `system_log` **e** `scrape_run`/`scrape_user_log` oltre `log_retention_days` (default 90; lo storico prezzi non si tocca mai). *Verifica: `test_system_log` (handler filtra le sorgenti, cursore due-modalità, filtri, purge, admin-only) — verde.*
- [x] **4.B8 — Cache di scrape: read-through** ✅ (0.4.0): modello `scrape_cache` (UNIQUE(plugin_id,cache_key), INDEX expires_at) + modulo **swappabile** `src/core/scrape_cache.py` (`ScrapeCache`: `get`/`put`; `cache_key` = sha256(plugin_id|method|URL normalizzata con query ordinata, fragment rimosso); emivita di default `DEFAULT_CACHE_TTL_MIN`=60; sessione propria per op, errori ingoiati → degrada a miss/no-op). Read-through nel `HttpClient` del contesto (import **type-only**, il client resta stdlib a runtime): **solo GET**, un hit non scaduto torna dalla cache **senza HTTP né attesa di politeness** e incrementa `cache_hits`; il GET reale di successo (2xx) viene salvato (i 4xx/5xx no). `cache_hits` cablato per-utente in `scrape_user_log` e sommato in `scrape_run` (sostituisce lo 0 placeholder di B6). Il `/test` (preview) usa la cache come tutto il resto (decisione 2026-06-26). Emivita 0 = cache disattivata. Purge/svuotamento = 4.B9; config per-plugin dell'emivita = 4.B10. *Verifica: `test_scrape_cache` (chiave normalizzata, miss→put→hit, ttl 0 disabilita, scaduto = miss, HttpClient serve dalla cache senza HTTP) — verde; la run idempotente del Dragon Store esercita il riuso.*
- [x] **4.B9 — Cache: pulizia + svuotamento** ✅ (0.4.0): in `src/core/scrape_cache.py` `purge_expired(session, plugin_id)` (cancella gli scaduti del plugin) chiamato dal worker **a inizio run** in `scraper_job` (POOL-R3, prima di aprire la `scrape_run`); `clear(session, plugin_id)` (svuota tutto) dietro `DELETE /api/admin/scrapers/{id}/cache` (admin-only, → `{deleted: n}`, 404 scraper ignoto). L'emivita 0 = cache disattivata è già nella read-through di B8. La **config per-plugin** dell'emivita (chiave riservata `cache_ttl_min`) resta 4.B10; il **pulsante UI** *Svuota cache* resta 4.F5. *Verifica: `test_scrape_cache` (purge solo gli scaduti del plugin, clear solo del plugin), `test_admin_scrapers` (DELETE admin-only, 404 ignoto, `{deleted}` corretto + 0 al secondo giro) — verde.*
- [x] **4.B10 — Config admin degli scraper** ✅ (0.4.0): tabella `scraper_admin_config` ([schema](../4-capabilities/database/schema.md)) + API admin per le **chiavi riservate del core** (`politeness_delay_ms`, `http_timeout_s`, `cache_ttl_min`, **`scrape_now_min_interval_s`** — sostituisce la costante di [3.B11](phase-03-catalog-first-scrape.md)), lette dal contesto a ogni run/scrape (SCR-R15) ([plugin-configuration](../3-features/admin/plugin-configuration.md)). *Verifica: emivita cambiata via API → la run successiva la rispetta, senza riavvio; intervallo Scrape ora cambiato → il prossimo cooldown lo rispetta.* **Decisione (2026-06-26)**: `cache_ttl_min` per-plugin **sostituisce** la costante `DEFAULT_CACHE_TTL_MIN`=60 di 4.B8 — `build_context` la passa a `ScrapeCache(ttl_min=…)` leggendola da `scraper_admin_config`; `scrape_now_min_interval_s` **sostituisce** sia `SCRAPE_NOW_COOLDOWN_SECONDS` sia il **feature flag dev `scrape_now_cooldown`** (stopgap di 0.4.0 → da rimuovere quando atterra questo step).
- [ ] **4.B11 — Strumento di consistenza i18n (dev/CI)** (~1h): script che confronta in modo **smart** le chiavi usate nel codice (`$_('...')` / `_('...')`, inclusi i namespace dei plugin) con quelle presenti nei JSON di traduzione (`src/frontend/src/i18n/{en,it}.json` + gli i18n dei plugin), segnalando tre cose: chiavi **usate ma mancanti**, chiavi **presenti ma inutilizzate** (dead) e **disallineamenti tra locale** (set di chiavi diverso tra `en` e `it`). Gestisce le chiavi **dinamiche** (es. `` $_(`errors.${code}`) ``) con una allow-list di prefissi, per non generare falsi positivi. Eseguibile a mano e in CI (fa fallire la pipeline sul drift). **Motivazione**: con le modifiche nel tempo è facile dimenticare di allineare i JSON, lasciando stringhe in più o mancanti — es. `changePassword.done`, rimasta **orfana** dopo l'auto-login di 0.3.4. *Verifica: rimuovo l'uso di una chiave → lo strumento la segnala come inutilizzata; uso una chiave assente dal JSON → la segnala come mancante; chiave presente solo in un locale → disallineamento.*

### Frontend

- [x] **4.F0 — Banner disallineamento schema (dev)** ✅ (0.4.0): legge il feed **admin** `GET /api/admin/errors` e, se non vuoto, **impila una barra per voce** (componente generico `Banner`, rosso/ambra semitrasparente) **a destra della sidebar** con titolo e descrizione di ogni errore + un bottone **Copy Message** (copia il messaggio come JSON `{type,title,description}`, torna a "Copy Message" dopo 5s senza cambiare dimensione). Compare **solo all'admin** nello shell, a flag attivo e con drift. *Verifica: da admin con DB disallineato la barra appare a destra della sidebar; utente comune/anonimo o flag `false` → niente barra.*
- [x] **4.F1 — Editor degli slot** ✅ (0.4.0): sotto-pagina **`/admin/scrapers/schedule`** (figlia di *Scrapers* nella sidebar) — tabella per-scraper (icona · orari come chip `hh:mm:ss` con × · time-picker + Add · toggle Attivo); **guard solo-UI**: niente due orari (di *qualsiasi* scraper) a meno di 1 minuto (circolare a mezzanotte). Sotto, la **24-ore schedule** (`$lib/components/ScheduleTimeline.svelte`): 6 fasce da 4h, marker = **icona del plugin** cliccabile (rimuove l'orario), legenda + contatore, **now-marker** che legge `server_time` da `/api/health` **una sola volta** al mount e poi avanza in locale ogni secondo (hover → evidenzia il prossimo run + "will start in …"). *Verifica: slot modificato → la run parte all'orario nuovo.* Orari canonici `HH:MM:SS` (4.B2 esteso, vedi a0). **Decisione (2026-06-26)**: lo slot editor vive in una **pagina dedicata** (non nella sottopagina config `[id]`, scelta di Simone).
- [x] **4.F2 — Parametri riservati nella pagina admin dello scraper** ✅ (0.4.0): nuova area **Admin → Scrapers** = indice (`/admin/scrapers`, elenco con versione + riepilogo schedule) + sottopagina per-scraper (`/admin/scrapers/[id]`) con form per politeness, timeout HTTP, emivita cache e **intervallo minimo dello Scrape ora** (le chiavi riservate di 4.B10), via `GET`/`PATCH /api/admin/scrapers/{id}/config`. *Verifica: modifica da UI → effetto alla run/scrape successiva.* **Decisione (2026-06-26)**: questa pagina ospita anche il **pulsante Svuota cache (4.F5)** → chiama il `DELETE /api/admin/scrapers/{id}/cache` (4.B9). La sottopagina è la sede futura dello **slot editor (4.F1)** e dei campi dichiarati dal plugin (fase 7+).
- [ ] **4.F3 — Pagina Log: polling con cursore** (~1h): lista incrementale near-real-time ([system-logs](../3-features/admin/system-logs-and-maintenance.md)). *Verifica: gli eventi della run appaiono mentre gira.* **Decisione (2026-06-26)**: consuma `GET /api/admin/logs` già pronto (4.B7) — primo load = ultime N (senza `since`), poi poll con `since=<maxId>`. Aperte: cadenza del polling, posizione in sidebar, una o due pagine (con 4.F4).
- [ ] **4.F4 — Pagina Log: filtri + autoscroll + heartbeat** (~1h): filtri livello/source, auto-scroll pausabile, evidenza heartbeat. *Verifica: filtri applicati al volo; heartbeat visibile.* **Decisione (2026-06-26)**: **niente riga heartbeat** in `system_log` (scelta di 4.B7) → l'evidenza heartbeat/liveness arriva da `/api/health` + file heartbeat, non da una riga di log.
- [x] **4.F5 — Svuota cache** ✅ (0.4.0): pulsante con conferma sulla sottopagina admin dello scraper (4.F2) che chiama il `DELETE …/cache` (4.B9) e riporta quante voci ha rimosso. *Verifica: dopo lo svuotamento la run successiva rifà le richieste al sito.*
- [x] **4.F6 — Pagina admin Feature flags (dinamica)** ✅ (0.4.0): pagina **Admin → Feature flags** (`routes/admin/feature-flags/+page.svelte`, voce sidebar `nav.featureFlags`) che si **auto-genera** dal `GET /api/admin/feature-flags` — un riquadro per chiave, un campo per parametro, con **widget dedotto dal tipo del valore** (`number`→input numerico, `boolean`→checkbox, `string`→testo); **nessun nome di flag hardcodato**. **Save unico** → `PATCH` dell'intera mappa, poi rimostra l'effettiva; avviso che i flag sono **non-persistenti** (reset al riavvio del web). Admin-only (l'API lo è), pagina di debug. **Niente metadati nel JSON dei flag** (decisione): l'inferenza dal valore basta per i flag attuali (tutti interi); un `_meta` opzionale per-parametro **solo se** servirà un widget vincolato. Niente Reset (per azzerare → riavvio web). `getFeatureFlags`/`patchFeatureFlags` in `client.ts`, i18n `nav.featureFlags` + `admin.featureFlags.*` (en+it). *Verifica: svelte-check 343 file 0 errori; aggiungendo un flag a `KNOWN_FLAGS` compare in pagina senza toccare il frontend; modifica + Save → `PATCH` e valore effettivo aggiornato.*

- [ ] **4.F7 — Pagina admin Impostazioni di sistema (MNT-R3)** (~1h): editor delle `system_settings` **senza riavvio** (DB-first) — `scraper_run_timeout_min`, soglia recuperi (`catchup_warning_min`), **`log_retention_days`** (la finestra del purge giornaliero di 4.B7), grazia cancellazione utenti (`user_deletion_retention_days`). Backend nuovo: `GET`/`PATCH /api/admin/settings` su `SystemSettings` (chiavi note, default sicuri). *Verifica: cambio `log_retention_days` da UI → il purge del worker usa il nuovo valore senza riavviare; valori fuori range rifiutati.*

## Decisioni trasversali (sessione 2026-06-26)

> Memoria per i prossimi sviluppi, prese chiudendo 4.B7/4.B8/4.B9/4.F6.

- **Cache & log su Postgres, dietro interfacce swappabili.** `ScrapeCache` (`src/core/scrape_cache.py`) e l'handler + `list_logs` (`src/core/system_log.py`) sono il *seam*: un eventuale backend Redis sarebbe uno **swap localizzato**, senza toccare `HttpClient`/runner/endpoint. **Redis NON si introduce ora** (worker singolo, runner seriale, scala hobby): solo se è già nello stack per altro, o quando la scala lo impone. Per i **log** a scala la risposta è un log-stack (Loki/ELK), **non** Redis — gli Stream non indicizzano `level`/`source` e il cursore è già il PK incrementale di Postgres.
- **I feature flag dev sono stopgap, non config di prodotto.** `worker_tick.seconds` resta un comodo override di sviluppo; **`scrape_now_cooldown.seconds` è temporaneo** → superato da `scrape_now_min_interval_s` per-plugin (4.B10). Idem la costante `DEFAULT_CACHE_TTL_MIN`=60 → superata da `cache_ttl_min` per-plugin (4.B10). Quando 4.B10/4.F2 atterrano: **rimuovere il flag dev** e leggere i valori da `scraper_admin_config` nel `build_context`.
- **Pagina feature flags senza metadati nel JSON.** Il widget si deduce dal tipo del valore (number/boolean/string); si aggiunge un `_meta` opzionale per-parametro **solo se** servirà un widget vincolato (enum/select, min/max, decimale con step).
- **Retention configurabile da UI = 4.F7** (nuovo step): `system_settings` editabili senza riavvio (`log_retention_days` & co.) via `GET`/`PATCH /api/admin/settings`. Finché non c'è, il default 90 è in `SystemSettings` e modificabile via DB.
- **⚠️ Controlli PROTOTIPO della 24-ore (4.F1b) da decidere PRIMA di chiudere la Fase 4** (marcati `TEMP(4.F1)` in `ScheduleTimeline.svelte`): (1) lo **slider dimensione marker** (8–24px) serve solo a scegliere un valore fisso → poi sostituire con la costante decisa e rimuovere lo slider; (2) l'**interruttore tacche orarie** on/off → decidere se tenerlo o fissare le tacche sempre visibili e rimuoverlo. Entrambi NON sono config di prodotto.

## Definition of Done

- [ ] Scraping quotidiano completamente automatico, **un solo scraper alla volta**, con scrape-now che convive (stessi lock).
- [ ] Scenari di resilienza provati: worker giù sullo slot (recupera), run oltre timeout (terminata e marcata, la coda riparte), errore su un utente (run `partial`).
- [ ] La cache lavora: stessa query tra due utenti o due run entro l'emivita → una sola visita al sito (visibile dai contatori `http_requests`/`cache_hits`); l'emivita si governa dalla UI admin.
- [ ] L'admin risponde a "quando ha girato? com'è andata? quanto ha fatto?" senza guardare il DB.
- [ ] [docs](../../docs/) aggiornata in inglese con la sola parte implementata in questa fase (DOC-12).

## Riferimenti

[scheduling-and-execution](../2-architecture/scheduling-and-execution.md) · [scraper-scheduling-and-limits](../3-features/admin/scraper-scheduling-and-limits.md)
