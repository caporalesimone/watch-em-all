# Fase 10 — Governo admin

> Stato: 🚧 in corso (avviata 2026-08-01) · Prerequisiti: Fase 4 (Fase 9 consigliata: più dati da osservare) · [Indice del flusso](README.md)

## Obiettivo

Completare la plancia dell'admin: statistiche e drill-down delle run, **i contatori di sempre per scraper** che la Fase 9 raccoglie e nessuno ha ancora mostrato, limiti di sistema modificabili a caldo, gestione utenti completa (con cancellazione differita), dashboard di sistema, messaggi agli utenti, manutenzione (retention e purge).

## Risultato apprezzabile

L'admin apre il monitoraggio e capisce in un colpo d'occhio quanto lavorano gli scraper (durate, richieste, trend), vede la giornata pianificata nella vista calendario, scende nel dettaglio per-utente di una run lenta, regola timeout e retention dalla UI, crea e gestisce gli account.

## MVP

> **Ordine dei blocchi, deciso da Simone il 2026-08-02:** apertura → **utenti** (10.B1–10.B5, 10.F1/10.F2) → **messaggi admin** (10.B12–10.B17, 10.F9–10.F11) → **monitoraggio e statistiche** (10.B6, 10.B20/10.B21, 10.F3–10.F5, 10.F15) → **10.D1 per ultimo**, dopo che tutto il resto è chiuso. Il resto (impostazioni, dashboard, calendario, config plugin-declared) si incastra dove serve.

### Apertura di fase (da fare per primi, 2026-08-02)

- [x] **10.X1 — Il pacchetto DDL della fase, tutto insieme** (~15m): le uniche colonne nuove su tabelle **esistenti**, e quindi l'unica ricreazione del database. Sono **due**: `users.password_changed_at` (serve a 10.B19) e `users.last_broadcast_read_id` (serve a 10.B12). *(Titolo e perimetro allargati il 2026-08-02: nasceva come "la colonna `password_changed_at` da sola", poi la decisione sul broadcast a riga singola ne ha aggiunta una seconda — presa in tempo, **prima** del `down -v`, che è esattamente il punto dell'MVP: se fosse arrivata dopo sarebbero state due ricreazioni.)* Scandendo i 38 MVP, tutto il resto costa zero schema: `admin_message` (10.B12) e `system_message_template` (10.B16) sono **tabelle nuove**, che `create_all` crea da sé; `password_expiry` (10.B19) sta in `system_settings`, che è **key→JSON** ([settings.py](../../src/core/settings.py)) e quindi vuole solo un campo sul modello Pydantic; `deletion_marked_at`, `deletion_due_at` e `last_login_at` **esistono già** su `User`. Restano `password_changed_at` (10.B19) e `last_broadcast_read_id` (10.B12): quest'ultima è **nullable**, perché *"non ho mai letto un annuncio"* è un'affermazione diversa da *"ho letto fino al numero zero"*, e **senza foreign key** verso `admin_message`, che è una tabella che nascerà un MVP più tardi — un vincolo non può puntare a ciò che non esiste ancora, ed è lo stesso motivo per cui `price_history` si lega all'identità invece che alla riga. **Il momento in cui la si aggiunge non è indifferente**: la [Fase 10b](phase-10b-statistics.md) esiste apposta per aspettare che i contatori per prodotto di `9.B6b` accumulino settimane di dati veri, e un `down -v` li azzera. Il database è stato ricreato il 2026-07-29: farlo oggi costa **quattro giorni**, farlo con 10.B19 verso fine fase ne costa **due mesi**, buttando via proprio l'attesa che la 10b aveva programmato. Quindi la colonna entra ora, **staccata dalla sua logica** — nessun enforcement, nessuna impostazione: solo la colonna, scritta a ogni cambio password, così 10.B19 atterrerà senza una seconda ricreazione. È `9.X6a` applicata **prima** di ripetere l'errore invece che dopo. *Verifica: `down -v` + `up` → la colonna esiste; un cambio password la valorizza; nessun comportamento nuovo al login.*
  > **Codice atterrato il 2026-08-02, casella ancora aperta:** la colonna è su `User` con `server_default=func.now()` (stessa forma di `created_at`, quindi i tre punti di inserimento — bootstrap admin, creazione da admin, e più avanti il reset di 10.B1 — la ricevono senza scriverla) e il cambio password la sposta esplicitamente ([auth.py](../../src/web/routers/auth.py)). **Non nullable per scelta**: un `NULL` avrebbe significato due cose diverse — *"mai cambiata"* e *"precede la colonna"* — e 10.B19 misura un'età, quindi non può permettersi l'ambiguità. Test `test_password_changed_at_follows_the_password`. **Resta il `down -v`**, che è di Simone: fino ad allora la guardia schema-drift di 4.B0 segnalerà la colonna mancante, ed è esattamente il suo mestiere.

### Catalogo (rifinitura, chiesta il 2026-07-31)

> Non è governo admin: è una rifinitura di una pagina **utente** che Simone ha chiesto di fare all'inizio di questa fase. Sta qui per scelta di sequenza, non perché appartenga al tema.

- [x] **10.F0 — La colonna Source mostra l'icona, non il nome** (~30m): nella tabella del catalogo la provenienza occupa oggi una colonna di testo — icona **più** nome dello scraper ripetuti su ogni riga, per un'informazione che in un'installazione con un solo negozio è costante e in una con tre si riconosce a colpo d'occhio dal logo. **È l'icona a portare il riconoscimento**, e per questo va ingrandita fino alla dimensione del riquadro dell'anteprima prodotto: le due celle si allineano, e un logo a 16px non identifica niente — togliere il nome *e* lasciare l'icona piccola renderebbe la colonna meno leggibile di adesso, non più. Il **tooltip** col nome al passaggio del mouse è un aiuto in più, non il canale dell'informazione (decisione di Simone, 2026-07-31). Due vincoli di implementazione: `SourceTag` è condiviso (catalogo, dettaglio carrello, pagine degli scraper), quindi la variante "sola icona" va aggiunta come **opzione del componente**, non copiata, o le tre pagine divergeranno alla prima modifica; e l'`alt` dell'immagine porta comunque il nome dello scraper — non per ridondanza col tooltip, ma perché un'icona con `alt` vuoto per uno screen reader **non esiste**, e quella cella resterebbe muta invece di ridondante. Costa un attributo. *Verifica: la colonna mostra solo il logo alla stessa altezza dell'anteprima, l'hover ne dice il nome, uno screen reader legge il nome dello scraper, e le altre pagine che usano `SourceTag` non cambiano aspetto.* **Fatto 2026-08-02:** prop `iconOnly` su [`SourceTag`](../../src/frontend/src/lib/components/SourceTag.svelte), non un secondo componente — le altre tre pagine che lo usano (dettaglio carrello, digest in-app, e il catalogo prima di oggi) non passano nulla e restano identiche. Il logo va a `h-10 w-10`, la misura esatta di [`ProductThumb`](../../src/frontend/src/lib/components/ProductThumb.svelte), con `object-contain` e non l'`object-cover` della miniatura: un logo ritagliato è un logo sbagliato. Il `title` col nome c'era già. Due cose decise scrivendo: l'`alt` **non** si riempie sempre ma solo in modalità sola-icona — accanto al nome scritto uno screen reader annuncerebbe il negozio due volte, da solo invece l'immagine *è* l'informazione; e se un plugin **non dichiara un'icona** la modalità compatta si disattiva da sé e torna il nome, perché una cella vuota sarebbe peggio di una verbosa. L'intestazione di colonna resta, ordinabile. *Verificato: `svelte-check` 443 file 0 errori, i18n consistente, prettier pulito. Resta la prova visiva nel browser, che è tua.*

### Le statistiche per scraper, assorbite dalla ex Fase 9b (deciso il 2026-08-02)

> La Fase 9 ha raccolto due famiglie di contatori e non ne ha mostrato nessuno (`9.B6b`, `9.B6c`): la rappresentazione era rimandata a una fase "9b — Statistics". Quella fase **si scioglie**: la metà **per scraper** arriva qui, perché è una pagina admin e questa fase la stava già costruendo (`10.B6`, `10.F5`); la metà **per prodotto** è una pagina utente accanto al grafico dei prezzi, non cade nella Definition of Done di questa fase, e vive nella [Fase 10b](phase-10b-statistics.md).

Cosa c'è già nel database, scritto da [`scraper_stats.py`](../../src/core/scraper_stats.py) — una riga per `plugin_id`, **globale** (non per utente), cumulativa e **mai potata**:

| Gruppo | Contatori | A cosa risponde |
|---|---|---|
| **Attività** | `runs_total`, `runs_ok`, `runs_failed`, `runs_skipped_locked`, `last_run_at`, `last_success_at`, `last_failure_at`, `consecutive_failures` | quante volte ha girato, e soprattutto se sta fallendo **adesso** o ha fallito una volta a marzo |
| **Traffico** | `http_requests_total`, `cache_hits_total`, `bytes_downloaded_total`, `politeness_wait_s_total`, `run_seconds_total` | quanto pesiamo sul sito, quanto rende la cache, e quanta parte di una run è **pura attesa di cortesia** |
| **Salute verso il sito** | `rate_limited_total`, `gate_hits_total`, `gate_cleared_total`, `robots_denied_total` | è ciò che è mancato durante il blocco del 2026-07-25, quando la domanda era "da quando succede e quanto spesso" |
| **Resa** | `products_delivered_total`, `pages_fetched_total`, `parse_failures_total` | quanto rende una richiesta, e se il parser sta perdendo pezzi in silenzio |

**Due sorgenti, due domande diverse, e vanno tenute distinte in pagina** — è la ragione per cui `10.B6` e `10.B20` sono due MVP e non uno. `scrape_run` **ha retention** (potata oltre `log_retention_days`, default 90): è una *finestra recente*, e serve per i trend, perché ha un asse dei tempi. `scraper_stats` è *memoria lunga* senza asse dei tempi: graficarla darebbe una retta. Mostrarle come se fossero la stessa cosa produce due numeri diversi per la stessa domanda.

> **Buco noto, da chiudere qui** (verificato il 2026-08-02): `record_run` è chiamato **solo dal worker** ([worker/main.py:265](../../src/worker/main.py#L265)). Lo *Scrape ora* manuale e l'aggiunta di un watch passano da [scrape.py:82](../../src/web/routers/scrape.py#L82), che chiama `run_for_user` direttamente e **non registra attività né traffico** — mentre i contatori scritti dal plugin (gate, rate limit, parse, pagine) contano comunque. Risultato: `http_requests_total` e `pages_fetched_total` raccontano storie diverse sullo stesso traffico. Va sanato in `10.B20`, prima di mettere quei numeri in una pagina: un contatore esposto è un contatore di cui qualcuno si fida.

### Backend

- [x] **10.B1 — Utenti: create/reset/disable** (~1h): API con invalidazione token (`token_version`), login a credenziali corrette su disabilitato → codice `account_disabled` (AUTH-R10) ([user-management](../3-features/admin/user-management.md)). **Decisione di Simone, 2026-08-02 — un admin non agisce mai su sé stesso**: disabilitare e cancellare il **proprio** account è vietato lato server, perché con un solo amministratore sarebbe un lockout senza percorso di recupero applicativo (resterebbe solo la riga nel database). Sugli **altri admin** invece può tutto: crearne, disabilitarli, cancellarli — la protezione riguarda l'identità di chi fa la richiesta, non il ruolo del bersaglio, ed è quindi una riga sola nella dipendenza. *Verifica: utente disabilitato → fuori entro la scadenza dell'access token; l'admin su sé stesso → rifiutato; lo stesso admin su un secondo admin → consentito.*
- [x] **10.B2 — Lista utenti ordinabile e filtrabile** (~1h): lista ordinabile per ultimo accesso (USR-R13), filtro `?status=` attivo/disabilitato/in-cancellazione (USR-R14). **Ristretto il 2026-08-02**: la metà "`last_login_at` aggiornato al login" **è già fatta** — la scrive [auth.py:81](../../src/web/routers/auth.py#L81) ed è già esposta in `AdminUserSummary`. Oggi [`list_users`](../../src/web/routers/admin_users.py) fa un `order_by(username)` secco e non accetta parametri: manca solo quello. Nota sul filtro: "in cancellazione" **non è una colonna di stato** ma la combinazione `deletion_marked_at is not None`, che è già la condizione con cui il login rifiuta l'accesso ([auth.py:77](../../src/web/routers/auth.py#L77)) — va derivata lì, non aggiunta al modello. *Verifica: ordinamento individua gli inattivi; filtro corretto.*
- [x] **10.B3 — Cancellazione soft con scadenza** (~1h): delete = disattiva + `deletion_marked_at` + `deletion_due_at`; restore = annulla → **disabilitato** (mai attivo). **La notifica di cortesia NON sta qui** (riposizionata il 2026-08-02): il messaggio `user.marked_for_deletion` con il suo `{deletion_due_date}` è **già dichiarato da 10.B16**, che è il posto dove vivono catalogo e risoluzione dei messaggi di sistema. Scriverlo cablato qui e rifarlo là sarebbe lo stesso testo in due posti, libero di divergere — e questa fase la lezione ce l'ha già dalla `9.F8`. La meccanica del ciclo di vita resta nel blocco utenti perché tutto il resto del blocco ci poggia sopra; la notifica arriva col blocco messaggi, che nell'ordine scelto viene subito dopo. *Verifica: delete non elimina alcun dato e fissa la scadenza; annullo → disabilitato.*
- [x] **10.B4 — Hook `delete_user_data` nei plugin** (~1h): metodo su BasePlugin (default no-op) + implementazione nei plugin con tabelle proprie. **Verificato il 2026-08-02, e il rischio che temevo non c'è**: cancellare un utente cancella le sue righe di `products` (il catalogo è per-utente), e la preoccupazione era che la cascata portasse via anche lo **storico prezzi**, che dalla fase 9 appartiene al *prodotto* e non a chi lo guarda — distruggendolo si distruggerebbe il dato di tutti gli altri. Non può succedere: `price_history` **non ha una foreign key verso `products`**, è legata all'identità `(plugin_id, external_id)`, e il commento nel modello dice che è precisamente per questo. Nessuna decisione da prendere, solo un invariante da non rompere: `delete_user_data` e la cascata core **non toccano `price_history`**. *Verifica: chiamata sul demo/Dragon Store → righe utente del plugin sparite; lo storico prezzi dei prodotti che seguiva è ancora lì.* **Fatto 2026-08-02, ed era quasi tutto già lì**: `delete_user_data` esiste su `BasePlugin` con default no-op dalla fase in cui è stato dichiarato, e **entrambi** i plugin con tabelle per-utente lo implementano — Dragon Store cancella i suoi `Watch`, TP Scraper i suoi `GeneratedProduct`. Mancava la sola cosa che rende un contratto un contratto: **un test**. Aggiunto su Dragon Store con due utenti che seguono lo stesso prodotto, che prova le tre affermazioni insieme — le righe di uno spariscono, quelle dell'altro restano, e il conteggio di `price_history` non si muove.
- [x] **10.B5 — Purge automatico utenti** (~1h): job giornaliero del worker (CRON-R10) per gli account con `deletion_due_at` scaduta: plugin in sequenza, cascata core solo se tutti ok, fallimento → utente resta marcato + errore in system_log, retry il giorno dopo (USR-R9/R10). *Verifica: account scaduto → eliminato senza righe residue; plugin che fallisce → dati intatti e nuovo tentativo.*
- [x] **10.B6 — API run: elenco e dettaglio per utente** (~1h): elenco run paginato e filtrabile per scraper, dettaglio delle righe `scrape_user_log` di una run. La sorgente è `scrape_run`/`scrape_user_log`, quindi **la finestra di retention e non la storia** (oltre `log_retention_days` le righe spariscono); i cumulativi di sempre sono `10.B20` e le due non vanno mescolate. **Spezzato il 2026-08-02** (regola 6): la parte "serie per i trend" è ora `10.B6b`, perché ha una decisione aperta sull'aggregazione mentre elenco e drill-down non ne hanno nessuna e non devono aspettarla. *Verifica: dati coerenti con le run accumulate; una run `partial` lascia individuare l'utente fallito col suo errore.* **Fatto 2026-08-02:** router nuovo [`admin_runs.py`](../../src/web/routers/admin_runs.py) con `GET /api/admin/runs` (paginato, filtrabile per scraper e per esito) e `GET /api/admin/runs/{id}`. Tre scelte che valgono più del CRUD: l'ordinamento usa `run_id` come **spareggio** oltre a `started_at`, perché due run possono partire nello stesso secondo — uno scrape manuale accanto a uno schedulato — e un confine di pagina che cade fra due righe che il database considera uguali mostrerebbe una delle due due volte; il drill-down mette **i falliti in cima**, perché su una run `partial` trovarli è tutto il motivo per cui la si apre; e la join sull'utente è **left**, così una run passata non cambia aritmetica quando l'account che conteneva viene poi cancellato (`scrape_user_log` tiene un `user_id` semplice, non una foreign key). Il `total` è di tutto e non della pagina, o la paginazione non si può disegnare.
- [ ] **10.B6b — API serie per i trend** (~1h): `GET /api/admin/scrapers/{id}/stats` con le serie che alimentano i grafici di `10.F5`. **Decisione aperta, da chiudere prima di scrivere**: la serie aggrega **per run** o **per giorno**? E soprattutto — `scrape_run` viene potata, quindi una serie non può guardare più indietro della retention; se si vuole un andamento che sopravviva alla potatura serve un **aggregato giornaliero scritto prima che le righe spariscano**, che è la domanda che la ex Fase 9b lasciava aperta e che nessuno ha ancora chiuso. Con `log_retention_days` a 90 e il database ricreato il 2026-07-29, le prime righe iniziano a sparire **verso fine ottobre 2026**: la decisione non è urgente oggi, ma un aggregatore che nasce dopo quella data nasce già senza il passato. *Verifica: la serie dichiara il periodo che copre e non finge di coprirne uno più lungo.*
- [x] **10.B7 — Il periodo di grazia vale solo per le marcature nuove** (~30m): **ristretto il 2026-08-02.** Il grosso di questo MVP **c'era già da 4.F7**: `GET`/`PATCH /api/admin/settings` esistono ([admin_system.py](../../src/web/routers/admin_system.py)), leggono e scrivono un `SystemSettings` tipizzato con range validati, `user_deletion_retention_days` è fra le chiavi (default 30) e l'effetto è a caldo perché il worker rilegge a ogni run. Nessun "seed" serve: `system_settings` è **key→JSON** e i default vivono nel modello Pydantic, quindi una chiave non scritta *è* il suo default. Resta solo la semantica dichiarata qui e mai implementata: cambiare il periodo di grazia **non deve spostare la scadenza degli account già marcati** — `deletion_due_at` si calcola all'atto della marcatura (10.B3) e da lì è un fatto, non una formula rivalutata ogni volta. *Verifica: grazia cambiata → vale solo per le nuove marcature, quelle in corso tengono la loro data.*
- [x] **10.B8a — La finestra notturna di manutenzione** (~1h, ridefinita da Simone il 2026-08-02): esiste **un solo momento** in cui girano tutte le attività di manutenzione e calcolo, a un'ora configurabile (`maintenance_hour`, default **7 del mattino**, letta nel fuso dell'installazione come gli slot di 4.B3). Prima la manutenzione partiva al primo tick dopo la mezzanotte UTC, cioè *quando capitava che il worker ripartisse*: a un'ora che nessuno aveva scelto, e più volte al giorno se il container veniva riavviato. La finestra **si annuncia a entrambi i capi** — una manutenzione che inizia e non dice di essere finita è la forma che un blocco ha in un log, e senza la riga di chiusura un admin non distingue un guasto da una notte tranquilla; la riga finale sta in un `finally`, così c'è anche se qualcosa dentro è caduto. **Recupera invece di saltare**: worker giù alle 07:00 e su alle 09:00 → la manutenzione di oggi si fa lo stesso. Dentro ci sono già la retention di log e run (4.B7) e il purge degli account scaduti (10.B5). *Verificato: `_maintenance_due` è puro e ha quattro test — prima dell'ora no, dopo sì, già fatta oggi no, e il recupero dopo un riavvio.*
- [x] **10.B8b — Il purge degli alert va a quantità, non a età** (~30m, deciso da Simone il 2026-08-02): `alert_keep_last` (default **100**, 0 = tieni tutto) e ogni notte la storia di ciascuno viene tagliata alle sue N notifiche più recenti. **Contate e non invecchiate**, che è la differenza deliberata rispetto alla retention di log e run lì accanto: quelle misurano rumore operativo, dove *"più vecchio di 90 giorni"* è la domanda giusta. La storia delle notifiche di una persona non è rumore — un mese tranquillo non deve svuotarla e una settimana rumorosa non deve renderla illeggibile. Un tetto risponde a entrambe. **Per utente, non globale**: un account pesante spingerebbe fuori la storia di tutti gli altri. L'ordinamento usa `created_at` **più l'id** come spareggio, perché due digest scritti nello stesso secondo sono normali (uno scrape notifica più persone) e la data da sola renderebbe ambiguo il taglio proprio dove conta. *Verificato: il tetto per-utente su un account rumoroso e uno tranquillo, e lo zero che tiene tutto.* **Niente più `DELETE /api/admin/alerts?before=`**: la potatura è automatica e a quantità, quindi l'admin sceglie il numero, non spara l'azione.
- [x] **10.B9 — Dashboard: aggregati globali** (~1h): `GET /api/admin/dashboard` — totali di sistema, statistiche notifiche ([admin-dashboard](../3-features/admin/admin-dashboard.md)). *Verifica: i totali tornano con i dati delle tabelle.* **Fatto 2026-08-02:** [`admin_dashboard.py`](../../src/web/routers/admin_dashboard.py), `GET /api/admin/dashboard?window_days=`. Tre decisioni dichiarate nel codice: i tre stati degli utenti **non fanno somma** con il totale, perché un account marcato è anche inattivo e contarlo in entrambi è la risposta onesta a due domande diverse invece che una partizione sbagliata; le consegne si contano **attraverso il loro alert** e non per il proprio timestamp, così una consegna rimasta pendente a cavallo della mezzanotte resta attaccata al digest che l'ha prodotta, che è la run che un admin andrebbe a cercare; e i due `skipped` sono **un numero solo**, perché dall'altitudine della dashboard *"nessun canale configurato"* e *"canale spento"* rispondono la stessa cosa a *"è uscito?"*. C'è un test che legge la risposta grezza e verifica che **nemmeno uno username** ci finisca dentro (DASH-R6).
- [x] **10.B10 — Dashboard: ranking utenti** (~1h): `/dashboard/users` — ranking per utente e per (utente, scraper), `http_requests` per-utente da `scrape_user_log`. *Verifica: nessun contenuto utente nelle risposte (DASH-R6).* **Fatto 2026-08-02:** `GET /api/admin/dashboard/users?window_days=`, con due classifiche — per utente e per coppia (utente, scraper). Due cose che il codice dichiara: le righe **possedute** (prodotti, carrelli) non hanno finestra temporale perché un catalogo è uno stato corrente e non un evento, mentre il **traffico** ce l'ha per forza — esce da `scrape_user_log`, che la manutenzione pota, quindi la finestra non è una comodità ma l'unico periodo di cui quei numeri possano parlare. E un account con watch ma senza run nella finestra **resta in elenco a zero**: *"ultimamente non costa niente"* è una risposta, mentre ometterlo si legge come *"non esiste"*. Sulla coppia (utente, scraper) i prodotti valgono zero e non un totale: un prodotto appartiene a un catalogo, non a una coppia. Un test verifica che le righe non contengano **nessun campo** oltre a numeri e username (DASH-R6).
- [x] **10.B11 — Interruttore globale notifier** — **già fatto in fase 7, verificato il 2026-08-02**: colonna `enabled` su `notifier_admin_config`, `GET` che la espone e `PATCH /api/admin/notifiers/{id}` come kill-switch ([admin_notifiers.py:96](../../src/web/routers/admin_notifiers.py#L96)), più il bottone abilita/disabilita nella pagina admin dei notifier ([admin/notifiers](../../src/frontend/src/routes/admin/notifiers/+page.svelte)). Il documento di [fase 7](phase-07-email-notifier.md) lo dichiarava già in apertura — *"toggle admin per-notifier (kill-switch, PCFG-R8) portato in fase 7, era rinviato a fase 10"* — perché serviva a governare l'in-app, che non ha gate di config; questa fase non se n'era accorta e lo teneva ancora a fare. Backend e frontend sono entrambi in piedi: qui non resta niente.
- [x] **10.B12 — Messaggi admin: invio** (~1h): tabella `admin_message`, kind `admin_message`/`system_message` su `alert_log`, `POST /api/admin/messages` a tutti/un utente sulla pipeline notifiche esistente ([admin-notifications](../3-features/admin/admin-notifications.md)). **Decisione di Simone, 2026-08-02 — un broadcast è UNA riga, e ogni utente porta un puntatore all'ultimo letto**, non N copie in `alert_log`. Il costo di un annuncio smette di dipendere da quanti utenti ci sono, e lo stato di lettura resta privato come vuole ADMSG-R5. Tre conseguenze da mettere in conto, tutte accettabili ma nessuna gratis: **(1)** il puntatore rende la lettura **monotona** — segnare letto un annuncio recente marca letti anche i precedenti, perché "letto fino a N" è l'unica cosa che un puntatore sa dire; per gli annunci va bene, per gli alert no, quindi i due meccanismi restano distinti; **(2)** il conteggio dei non-letti in sidebar diventa la **somma** di due sorgenti (righe `alert_log` non lette + annunci dopo il puntatore) e lo storico utente una **unione**, quindi il risparmio è in scrittura e il prezzo è in lettura; **(3)** il puntatore **non** copre gli esiti di consegna di 10.B13 — sapere se la mail è arrivata *a quel destinatario* resta per-utente, quindi `alert_delivery` continua ad avere N righe. Il messaggio a un singolo utente non usa il puntatore: ha già un destinatario solo. *Verifica: utente senza canali riceve il messaggio in-app (ADMSG-R2); un broadcast a N utenti scrive una riga sola; il badge non-letti li conta comunque.* **Fatto 2026-08-02:** contratto `TextMessageEvent` (AEV-R6/R7), tabelle `admin_message` e `admin_message_delivery`, `POST /api/admin/messages`, core in [`admin_messages.py`](../../src/core/admin_messages.py), drain gemello in [`notify.py`](../../src/core/notify.py). **Gli esiti hanno una tabella propria e non `alert_delivery`**: un broadcast non ha una riga di `alert_log` a cui appenderli, e l'esito è per **utente** — l'unica cosa che `alert_delivery` non ha mai dovuto sapere, perché un digest appartiene a una persona sola per costruzione. La regola sui canali (chi è attivo, chi è in-app, il marcatore `skipped_no_notifier`) è stata **estratta** in `channel_outcomes()` invece di essere riscritta: serve a entrambe le tabelle ed è esattamente il tipo di regola che non deve esistere due volte. **Una decisione cambiata scrivendo i test**: l'idea era limitare gli annunci visibili a quelli successivi alla creazione dell'account con un confronto sulle date, e il test è passato in un caso e fallito nell'altro — su SQLite i timestamp si confrontano **come testo**, e un `datetime` Python bindato porta i microsecondi che `CURRENT_TIMESTAMP` non scrive. Il puntatore ora **parte dall'ultimo annuncio** al momento della creazione dell'account: stessa semantica, un `id` invece di una data, e nessuna differenza di comportamento fra SQLite e Postgres. La firma `send()` dei notifier diventa un'unione (`AlertEvent | TextMessageEvent`): il render vero è 10.B15, quindi fino a lì l'email registra un fallimento leggibile invece di consegnare una mail mezza formattata. **Incluso qui anche il lato lettura**, perché è la conseguenza dichiarata della riga singola e non un MVP a parte: `GET /api/alerts` unisce le due sorgenti e ogni riga porta un `source` (`alert` | `broadcast`), dato che i due `id` vivono in tabelle diverse e da soli non identificano più una notifica; il merge è in Python e se lo può permettere, perché la purge notturna tiene corta la lista degli alert e gli annunci sono rari. `GET /api/alerts/broadcasts/{id}` mostra l'annuncio con gli esiti di consegna **di chi lo chiede** — il messaggio è condiviso, la consegna no. Un annuncio precedente all'account resta visibile in archivio ma già letto: il badge non è un arretrato da smaltire. 9 test nuovi.
- [x] **10.B13 — Messaggi admin: esiti** (~1h): `GET /api/admin/messages` con esiti per destinatario/canale. *Verifica: l'admin non vede lo stato letto/non letto (ADMSG-R5).* **Fatto 2026-08-02:** elenco paginato con il tally per stato (una `GROUP BY` sola per pagina, non una query per riga) e dettaglio con i canali di ogni destinatario, **fallimenti in cima** — chi apre questa pagina la apre per sapere cosa è andato storto, e gli account raggiunti bene sono la parte da scorrere. `skipped_no_notifier` confluisce in `skipped`: per l'admin sono lo stesso fatto, cioè che non è uscito niente e la persona ce l'ha solo in-app. **ADMSG-R5 è scritto come test e non solo come commento**: nel payload non c'è lo stato di lettura, e non c'è nemmeno una query che potrebbe produrlo — la consegna è affare dell'admin, la ricezione no. Join in outer sull'utente perché un messaggio deve continuare a rendersi anche se un destinatario nel frattempo è stato cancellato. 3 test nuovi.
- [x] **10.B14 — Helper Markdown nel contesto** (~1h): `markdown.to_html()` (markdown-it-py + sanificazione nh3) e `markdown.strip()` (AEV-R7, CTX-R8). *Verifica: grassetto/lista → HTML corretto; HTML inline nel body non passa.* **Fatto 2026-08-02:** [`src/core/markdown.py`](../../src/core/markdown.py) con `to_html()` e `strip()`, esposti sul contesto come `context.markdown` (un attributo namespaced invece di due funzioni sciolte). Il testo passa **due sanificazioni**: il parser è configurato per *escapare* l'HTML grezzo invece di inoltrarlo, e il risultato passa comunque per `nh3` con una allow-list corta — niente immagini (una immagine remota in una mail è un pixel di tracciamento con un altro nome, e la fase 7 aveva già deciso solo link), niente tabelle, niente titoli. "Scritto da un admin" è un'affermazione su chi ha digitato, non su cosa c'è dentro. **Due cose imparate scrivendo i test**, entrambe correzioni alle mie assunzioni e non al codice: il markup ostile finisce **escaped e non cancellato** (il lettore vede i caratteri che ha scritto, ed è l'esito onesto per il corpo di un messaggio), e un link `javascript:` viene rifiutato **da markdown-it**, che non costruisce proprio l'anchor — una difesa più a monte di dove la cercavo. `strip()` è derivato dall'HTML reso e non da regex sul sorgente, così le due rese non possono divergere su cosa dice il messaggio, e la struttura a blocchi sopravvive come righe: una lista puntata resta leggibile come lista. Dipendenze aggiunte con `poetry add` (3 installazioni, 0 aggiornamenti: `markdown-it-py`, `nh3`, `mdurl`).
- [x] **10.B15 — Render Markdown nei notifier** (~1h): messaggi testuali resi via helper nei notifier esistenti, degradazione mai bloccante (NOT-R8). *Verifica: email formattata, testo pulito sui canali poveri; helper rotto → consegna comunque.* **Fatto 2026-08-02:** l'email rende il messaggio testuale con `context.markdown` — **il titolo è l'oggetto**, senza il contorno del digest, perché un annuncio incorniciato in "ecco i tuoi avvisi di prezzo" mentirebbe su cosa è. Gli helper arrivano da `initialize()` e non da un import diretto, perché `send()` non riceve il contesto e AEV-R7 vuole che la resa venga dal core e non da un parser del plugin. **La degradazione è codice, non intenzione**: se l'helper manca o solleva, il corpo esce escaped con gli a capo conservati e la mail parte lo stesso — un problema di formattazione non può essere il motivo per cui qualcuno non viene a sapere di una manutenzione programmata, che è esattamente il tipo di messaggio che questo canale porta. Il notifier in-app resta un no-op: lì la resa è del browser (10.F10). 3 test sull'email + 2 sul drain dei messaggi (consegna riuscita e fallita, con la ragione registrata e nessun ritentativo).
- [x] **10.B16 — Messaggi di sistema: catalogo e risoluzione** (~1h): registro chiavi+default nel core, tabella `system_message_template` (solo override), risoluzione unica override→default → punto di traduzione identità → placeholder, usata da tutti i call-site (ADMSG-R7..R10); `user.marked_for_deletion` dichiara `{deletion_due_date}` (USR-R11). *Verifica: override attivo → l'avviso di disattivazione usa il testo custom.* **Fatto 2026-08-02:** [`src/core/system_messages.py`](../../src/core/system_messages.py) con quattro chiavi — `user.disabled`, `user.marked_for_deletion` e le **due mail di credenziali**, che 10.B24 aveva scritto in casa propria con la nota di spostarle qui: spostate, non copiate. **Nessuna ricreazione del database**: `system_message_template` è una tabella *nuova*, e `create_all` le tabelle nuove le crea (è alterare quelle esistenti che non sa fare). Le tre cose che non sono ovvie. **Un placeholder sconosciuto rende come testo, non come eccezione** (ADMSG-R8): la sostituzione passa da un dizionario che al `__missing__` restituisce la chiave fra graffe, quindi un admin che scrive `{usernme}` consegna quei nove caratteri invece di far esplodere il server *nel momento in cui il sistema deve dire a qualcuno che l'account sta per sparire*. L'editor avverte; il runtime non punisce. **La traduzione è un passo con un nome anche se in V1 è l'identità** (ADMSG-R10), perché conta l'ordine: tradurre dopo aver sostituito significherebbe che un valore contenente graffe viene reinterpretato dallo stadio successivo. **La nota di cortesia viaggia sul percorso ordinario** — una riga di `alert_log` con le sue `alert_delivery`, esattamente come un digest — e non sulle tabelle dei messaggi admin: quelle sarebbero state una macchina da broadcast con una platea di uno, più una colonna per ricordarsi che non l'ha mandata nessuno. Ha richiesto una sola modifica al drain, che ora sceglie come interpretare il payload guardando il `kind` della riga invece di indovinarlo dai campi. La cortesia è **best-effort e dopo il commit**: un notifier irraggiungibile non deve far fallire la disabilitazione, lasciando l'admin nel dubbio se sia andata; la riga in-app resta comunque, quindi chi viene ripristinato trova la spiegazione ad aspettarlo. 7 test nuovi.
- [x] **10.B17 — API message-templates** (~1h): `GET/PUT/DELETE /api/admin/message-templates`, validazione dei placeholder in PUT. *Verifica: DELETE → torna il default; placeholder sconosciuto segnalato.* **Fatto 2026-08-02:** [`admin_message_templates.py`](../../src/web/routers/admin_message_templates.py). La forma della risorsa *è* il progetto: **la lista è il catalogo, non la tabella** — il `GET` restituisce ogni chiave che il core dichiara, ciascuna col suo default *e* col suo override se esiste, quindi un messaggio aggiunto al core compare qui appena viene scritto (ADMSG-R9). Il `DELETE` **toglie l'override** e non "ripristina": tornare al default è smettere di avere una riga, non copiarci dentro il testo di partenza — che è la differenza fra un default che resta aggiornato e uno congelato al giorno in cui qualcuno ha premuto il bottone. La validazione tratta i due problemi in modo diverso perché sono diversi: un placeholder **sconosciuto** è un avviso, viene salvato e consegnato come testo letterale; uno **obbligatorio mancante** è un rifiuto `422`, e riguarda solo le due mail di credenziali. I placeholder sconosciuti sono riportati anche **in lettura**: un override salvato prima che una chiave venisse rinominata nel core è ancora lì e ancora consegnato, e merita l'avviso la prossima volta che qualcuno apre la pagina. 5 test nuovi.
- [x] **10.B18 — API calendario scraper** (~1h): `GET /api/admin/scrapers/calendar?date=` — slot pianificati del giorno + durata media delle run recenti (SCHED-R10). *Verifica: gli slot rispecchiano gli schedule; sospesi esclusi o marcati.* **Fatto 2026-08-02:** `GET /api/admin/scrapers/calendar?date=`. Gli scraper sospesi **si restituiscono e si marcano**, non si filtrano: una giornata che sembra vuota perché è tutto spento è indistinguibile da una in cui non era schedulato niente, e sono due problemi molto diversi. La durata media è calcolata **in Python e non in SQL** — i due dialetti scrivono una differenza fra timestamp in modi diversi (`julianday` contro `extract(epoch …)`), e con quindici giorni di run comprare portabilità con un ciclo su una manciata di righe è il baratto giusto. Entrano solo le run **finite** e andate a buon fine: una non finita non ha durata, e una in timeout tirerebbe la media verso il guasto invece che verso quanto costa normalmente quella giornata. Nessuna run da mediare → **`null`**, non uno zero né un default: un valore inventato farebbe disegnare un blocco sicuro di sé attorno a una supposizione.
- [x] **10.B19 — Scadenza password configurabile (admin)** (~1h): impostazione di sistema `password_expiry` (in `system_settings`, 10.B7) con **opzioni fisse** — **Mai (default)**, 1 mese, 3 mesi, 6 mesi, 1 anno; nuova colonna `password_changed_at` su `users`, aggiornata a ogni cambio password; al login, se la password è più vecchia della finestra configurata, si **forza il cambio** riusando `must_change_password` e il flusso di cambio forzato già esistente. *Verifica: impostata a 1 mese → un utente con password più vecchia di un mese è forzato al cambio al login; "Mai" → nessun forzamento.*
- [x] **10.B20 — API statistiche cumulative per scraper + copertura dello scrape manuale** (~1h): `GET /api/admin/scrapers/{id}/lifetime-stats` espone la riga di `scraper_stats` con il suo **`since`** — che non è un dettaglio, è ciò che rende leggibile un cumulativo dopo un cambio di configurazione (caso vero: la cortesia da 1,5s a 11s in `0.8.1` rende i totali prima e dopo non confrontabili). Nello stesso MVP si chiude il buco dichiarato sopra: il percorso web (*Scrape ora* e aggiunta di un watch) deve registrare attività e traffico come fa il worker, altrimenti i contatori del core e quelli del plugin contano due traffici diversi. *Verifica: dopo uno scrape manuale i totali si muovono come dopo una run schedulata; `since` presente e coerente con la creazione della riga.* **Fatto 2026-08-02:** `GET /api/admin/scrapers/{id}/lifetime-stats`, e il buco chiuso alla radice. Il rimedio non è stato aggiungere la registrazione al percorso web ma **estrarla dove i due percorsi la condividono**: [`src/core/run_log.py`](../../src/core/run_log.py) possiede l'aritmetica — `open_run`, `run_one_user`, `close_run` — e sia il worker sia lo *Scrape ora* la usano. Copiarla nel web avrebbe creato una seconda implementazione degli stessi contatori, che è precisamente il modo in cui erano finiti a raccontare due traffici diversi. Al worker resta solo ciò che è davvero suo: la scadenza della finestra, il timbro del cooldown e la regola che il fallimento di un utente non ferma la coda. **Un dettaglio scoperto scrivendo il test**: il traffico più grosso del percorso web non è lo scrape manuale ma il **job che risolve la URL incollata**, e quello si costruisce un contesto HTTP proprio dentro il plugin — il drainer del core non lo vede. Quindi `record_traffic()` sta nel core e viene chiamato **due volte, da chi possiede il client**: dal drainer per il proprio, e da `_resolve_watch` per quello che si è creato. Traffico e basta, senza riga di run: risolvere una URL è lavoro verso il sito, ma non è una *run* — non ha delta utente né esito, e contarla gonfierebbe `runs_total` con qualcosa che non ha girato. Il filtro `trigger` (`scheduled` di default, `manual`, `all`) è sull'API e sulla pagina Run: gli scrape manuali sono registrati, ma non devono spingere fuori dalla prima pagina le run schedulate proprio nel giorno in cui qualcuno sta debuggando a mano. 2 test nuovi.
- [x] **10.B22 — Uno scraper dichiara la propria config** (~1h): **MVP nuovo, trovato il 2026-08-02 analizzando `10.F13`.** Quel MVP costruisce il form dinamico per "le chiavi dichiarate dal plugin nel suo schema" — ma **nessuno scraper dichiara uno schema**: `ScraperPlugin` non ha l'equivalente del `config_schema` che i notifier hanno da 7.B3, e le soglie degli adjustments di Dragon Store sono **cablate** in [`adjustments.py`](../../src/plugins/scrapers/dragon_store/backend/adjustments.py) dalla fase 5, dove era scritto "valori cablati in fase 5, admin-editabili in fase 7+/9" e non è mai successo. Senza questo, `10.F13` è un form senza niente da mostrare. Qui: il contratto (lo scraper dichiara i suoi `ConfigField`, accanto alle chiavi riservate del core di 4.B10, senza collidere), la persistenza in `scraper_admin_config`, e Dragon Store come primo consumatore che sposta le sue fasce di sconto e la spedizione dal codice alla config. *Verifica: le soglie cambiate via API cambiano gli adjustments della valutazione successiva senza riavvio; le chiavi riservate del core restano distinte da quelle del plugin.* **Fatto 2026-08-02:** `ScraperPlugin.get_admin_config_schema()` (default vuoto, stessa forma dichiarativa che i notifier hanno da 7.B3), persistenza nello stesso `scraper_admin_config.config_json` accanto alle chiavi riservate, e `GET`/`PUT /api/admin/scrapers/{id}/plugin-config`. **Solo admin, nessun livello per-utente**: sono impostazioni su come l'installazione tratta un sito, e a un sito non importa chi lo sta guardando. Quattro scelte che valgono più del CRUD. **La collisione con le chiavi riservate è un errore, non un commento di review**: `declared_schema()` solleva se un plugin dichiara `politeness_delay_ms` o simili — il core legge quelle chiavi *per conto* del plugin, e uno che ne ridefinisse una cambierebbe un comportamento che non possiede. **I due lati convivono nella stessa riga senza sovrascriversi**, ed è il guasto che merita il test: salvare le soglie non tocca la cortesia e viceversa. **`on_config_changed()` è un hook e non una rilettura**, perché solo il plugin sa cosa ha derivato da quei numeri — Dragon Store ne costruisce un oggetto di regole e se lo tiene, quindi il modo onesto di tenerlo fresco è dirgli che sono cambiati; è ciò che rende vero il *«senza riavvio»* del MVP. **I valori restano `Decimal`**: le fasce si confrontano con un totale carrello esatto, e uno `0.1` che in realtà è `0.1000000000000000055` decide una soglia al contrario ogni tanto. Dragon Store è il primo consumatore e sposta lì le sue tre fasce di sconto e la spedizione, cablate dalla fase 5 sotto una nota che prometteva esattamente questo trasloco. **L'ultimo parametro di cortesia cablato** — i tentativi di ripetizione — diventa `http_retries` fra le chiavi *riservate* e non fra quelle del plugin: è una manopola del core, e il core è chi la legge. 3 test nuovi.
- [x] **10.B21 — Azzeramento dei contatori cumulativi** (~30m): `POST /api/admin/scrapers/{id}/lifetime-stats/reset` — azzera i contatori e **ristampa `since`**. È la risposta alla domanda che la ex Fase 9b lasciava aperta: un cumulativo che non si azzera mai inganna dopo un cambio di configurazione, e l'alternativa (spiegare in didascalia che i numeri mescolano due regimi) chiede al lettore un lavoro che il bottone fa meglio. Distruttivo e senza storico: la conferma lo dichiara. *Verifica: reset → tutti i contatori a zero e `since` all'istante del reset; i contatori riprendono a salire dalla run successiva.* **Fatto 2026-08-02:** `POST /api/admin/scrapers/{id}/lifetime-stats/reset`. `reset_stats()` azzera **campo per campo** invece di cancellare la riga e lasciarla ricreare: una `DELETE` porterebbe via anche `consecutive_failures` e i tre timestamp, e *«sta fallendo adesso»* non è un cumulativo — è lo stato corrente dello scraper, ed è ancora vero il secondo dopo che qualcuno ha azzerato i totali.

### Frontend

- [x] **10.F1 — Pagina utenti: lista** (~1h): stato, **ultimo accesso ordinabile**, data marcatura e scadenza, **filtro stato** attivo/disabilitato/in cancellazione. *Verifica: ordinamento e filtro corretti da browser.*
- [x] **10.F2 — Pagina utenti: azioni** (~1h): creazione, reset password, abilita/disabilita, cancella (soft, con riepilogo e data di eliminazione programmata), **annulla cancellazione**. *Verifica: annullamento porta a disabilitato, mai ad attivo.*
- [x] **10.F3 — Monitoraggio run: elenco** (~1h): per-scraper: stato corrente, ultima run, elenco run ([scraper-monitoring](../3-features/admin/scraper-monitoring.md)). *Verifica: lo stato di ogni scraper è leggibile a colpo d'occhio.* **Fatto 2026-08-02:** pagina `/admin/runs`, figlia di *Scrapers* nella sidebar perché una run appartiene a uno scraper. Chip di esito, paginazione, e le colonne che rispondono alle tre domande del flusso di lettura admin — quando, com'è andata, quanto ha fatto — con l'esito **anche a colore**, così la colonna si scorre invece di leggerla. Una didascalia dichiara che è una **finestra recente**: senza, una pagina vuota su dati potati si legge come *"non ha mai girato"*, che è un'altra affermazione. Cambiare filtro riporta a pagina 1, perché la pagina 7 di un insieme più corto non esiste. *Verificato: `svelte-check` 0 errori, i18n consistente.*
- [x] **10.F4 — Monitoraggio run: drill-down** (~1h): dettaglio per-utente di una run. *Verifica: run partial → l'utente fallito è individuabile col suo errore.* **Fatto 2026-08-02:** il dettaglio si apre **in linea sotto la riga**, non su una pagina a parte: la domanda a cui risponde si fa solo mentre si guarda quella run, e navigare altrove perderebbe il filtro e la pagina in cui eri. I falliti sono in cima (l'ordine arriva da 10.B6) e l'errore è in rosso. Un account cancellato lascia dietro le sue righe, quindi il nome può essere nullo: la cella lo **dichiara** invece di restare vuota, perché una cella vuota si legge come un difetto e non come storia. *Verificato: `svelte-check` 0 errori, i18n consistente.*
- [ ] **10.F5 — Trend e contatori della finestra recente** (~1h): grafici durate/http_requests/variazioni per run (componente grafico riusato), contatori 7/30gg, da `10.B6`. Sono i numeri che hanno un **asse dei tempi**; i cumulativi di sempre sono `10.F15` e vanno in un blocco separato, non mescolati a questi. *Verifica: trend leggibili; è dichiarato che la finestra si ferma alla retention.*
- [x] **10.F6 — Le due impostazioni della finestra notturna** (~15m): **ri-ristretto il 2026-08-02** dopo la ridefinizione di 10.B8. La pagina `/admin/settings` esiste da 4.F7; qui arrivano `maintenance_hour` e `alert_keep_last` fra i suoi campi, con l'ora limitata a 23 invece che lasciata aperta — un `24` significherebbe in silenzio una finestra che non si apre mai. **L'azione manuale di purge non c'è più**: con la potatura automatica a quantità l'admin governa impostando il numero, non premendo un bottone. *Verificato: `svelte-check` 0 errori, i18n consistente.*
- [x] **10.F7 — Dashboard: statistiche globali** (~1h): aggregati di sistema e statistiche notifiche con finestra 7/30gg ([admin-dashboard](../3-features/admin/admin-dashboard.md)). *Verifica: numeri coerenti con i dati.* **Fatto 2026-08-02:** pagina `/admin/dashboard`, prima voce dell'area admin. Due famiglie di numeri tenute **visivamente separate**, perché non si leggono allo stesso modo: cosa l'installazione *è* adesso (nessuna finestra: un catalogo è uno stato) e cosa *ha fatto* su 7 o 30 giorni, con la finestra dichiarata sotto le cifre. Gli stati degli account sono scritti **sotto** il totale invece che come tre fette di esso: un account in cancellazione è anche inattivo, quindi le parti si sovrappongono per davvero e una torta sarebbe una bugia. Le righe di storico prezzi portano la loro didascalia — è l'unica tabella che nessuno pota mai. *Verificato: `svelte-check` 0 errori, i18n consistente.*
- [x] **10.F8 — Dashboard: ranking utenti** (~1h): dati e carico per utente e per scraper. *Verifica: solo numeri e username, nessun contenuto.* **Fatto 2026-08-02:** due tabelle in coda alla dashboard, per utente e per coppia (utente, scraper), che usano **la stessa finestra** del blocco notifiche sopra — così i numeri della pagina si leggono insieme invece che come due periodi scollegati. Una didascalia dice quale colonna è sulla finestra e quale è uno stato corrente, perché senza si sommerebbero due cose diverse a mente. Un account cancellato che possiede ancora righe di una run passata viene **dichiarato tale** invece di lasciare la cella vuota. Solo numeri e username, come l'API (DASH-R6). *Verificato: `svelte-check` 0 errori, i18n consistente.*
- [x] **10.F9 — Composizione messaggi admin** (~1h): textbox Markdown con **anteprima live** (markdown-it + DOMPurify), invio a tutti/un utente, elenco inviati con esiti. *Verifica: anteprima identica al render in-app.* **Fatto 2026-08-02, con due scostamenti dalla specifica, entrambi decisi da Simone.** **(1) L'anteprima la rende il server**, non markdown-it nel browser: `POST /api/admin/messages/preview` restituisce lo stesso `body_html` che riceveranno i destinatari, quindi *"identica al render in-app"* è vera **per costruzione** e non perché due implementazioni sono d'accordo — che è precisamente la condizione che 9.F8 ha dovuto disfare. Il test lo asserisce confrontando l'HTML dell'anteprima con quello consegnato, invece di fidarsi. In più il bundle non cresce di due dipendenze. **(2) Non è live**: tab **Scrivi / Anteprima** alla GitHub, la richiesta parte al click sul tab. Un'anteprima che una persona chiede vale una richiesta; una che si rende mentre digita ne vale qualche centinaio. La pagina è `/admin/messages`, sotto Notifiers nella sidebar: composizione in alto (destinatario, titolo, tab, invio con conferma che dice **a quante persone** sta andando, perché il messaggio è immutabile), elenco degli inviati sotto con il tally per stato e il drill-down per destinatario — che è ciò che rende utilizzabile **10.B13**, finora API e basta. `MessageBody` e `DeliveryList` generalizzati per servire anche qui invece di essere riscritti. 2 test nuovi.

  > **Regola aggiunta da Simone il 2026-08-02, dopo la prova nel browser — ADMSG-R1b:** gli **amministratori non ricevono** questi messaggi, né gli altri né chi scrive. Il broadcast li esclude dalla platea e un invio mirato a un admin è **rifiutato dall'API** (`422 recipient_is_admin`), non solo nascosto dalla tendina: una regola che vive in un widget è una regola che l'API non ha. La pagina lo dichiara sotto i radio, così un conteggio più basso del numero di account non si legge come un bug, e se *tutti* gli account fossero admin il pulsante è disabilitato con la ragione scritta. Ha richiesto di correggere cinque asserzioni che davano per scontato l'admin fra i destinatari — erano il modo in cui il vecchio comportamento era inciso nella suite. 2 test nuovi.

  > **Aperta, emersa provando un annuncio lungo davvero:** la allow-list di 10.B14 esclude i titoli, quindi un `## Sezione` viene **appiattito** — `nh3` toglie il tag e tiene il testo, che resta una riga senza margini incollata al paragrafo dopo. Ragionevole per un avviso di tre righe, meno per un comunicato con tre sezioni. Tre uscite: ammettere `h3`/`h4` resi in piccolo (un titolo dentro un messaggio è subordinato al titolo *del* messaggio); degradare `##` a paragrafo in grassetto; oppure lasciare così e dirlo nell'aiuto dell'editor. **Decisione a Simone.**
- [x] **10.F10 — Storico utente: categorie e render** (~1h): categorie sistema/admin, icona e colore dedicati ai messaggi admin, render Markdown dei messaggi testuali, filtro per categoria (ALERT-R16). *Verifica: broadcast formattato ed evidenziato nello storico di ogni utente.* **Fatto 2026-08-02:** chip di categoria sulla lista, badge viola con megafono sulle notifiche admin, titolo del messaggio al posto del conteggio carrelli (un digest **non** ha titolo, e resta `null` invece di inventarne uno), rotta `/alerts/broadcast/[id]` separata perché i due `id` vivono in tabelle diverse. **Il Markdown lo rende il server, non il browser**, ed è la decisione di progetto di questo MVP: il payload arriva con un `body_html` già sanificato dallo stesso helper che usa l'email. L'alternativa — markdown-it + DOMPurify nella pagina — è esattamente la forma che 9.F8 ha dovuto disfare una volta (la regola della Differenza in Python per la mail e in TypeScript per la pagina, finché non hanno smesso di essere d'accordo); così i due canali non possono raccontare il messaggio in modo diverso, e in più il bundle non cresce di due dipendenze. **Conseguenza per 10.F9**: l'anteprima live vorrà un endpoint di preview invece di un renderer nel browser — è l'unico modo perché "identica al render in-app" sia vera per costruzione e non per somiglianza. Estratti `MessageBody` e `DeliveryList`, quest'ultimo perché due copie della stessa lista di esiti avrebbero prima o poi litigato su cosa vuol dire "skipped". Le righe broadcast non hanno la spunta di cancellazione: sono condivise, non c'è niente da cancellare lì. 2 test backend nuovi.
- [x] **10.F11 — Tab messaggi di sistema** (~1h): lista del catalogo con stato default/override, editor Markdown con anteprima e placeholder mostrati, ripristino al default con conferma (ADMSG-R7/R8). *Verifica: override → ripristino → il testo torna quello del core.* **Fatto 2026-08-02:** [`SystemMessages.svelte`](../../src/frontend/src/lib/components/SystemMessages.svelte), montato come **secondo tab della pagina Messaggi** invece che come voce nuova in sidebar: scrivere alle persone e curare le parole che il sistema scrive da solo sono due lavori diversi, ma chi cerca *«le frasi che questa installazione manda»* le cerca in un posto solo. Una riga a fisarmonica per chiave, con il badge che dice se in vigore c'è il default o una riscrittura. L'editor è **la stessa coppia Scrivi/Anteprima** del compositore degli annunci e passa dallo stesso endpoint del server, quindi l'admin vede il testo come verrà consegnato e non una seconda approssimazione (la ragione di 10.F9). I placeholder sono **chip che si inseriscono da soli**: digitare `{deletion_due_date}` a mano è il modo in cui un placeholder sconosciuto finisce in un override, e il modo migliore di prevenirlo è non farlo digitare. Gli obbligatori portano un asterisco e la loro nota; quelli sconosciuti già salvati sono segnalati in cima alla riga. La bozza vive **solo per la riga aperta** — aprirne un'altra la scarta, di proposito: tenerne N vive invita a salvarne una che avevi dimenticato di stare modificando.
- [x] **10.F12 — Vista calendario del giorno** (~1h): blocchi per ogni run pianificata (dimensionati sulla durata media), read-only, click sullo scraper → la sua pagina di configurazione, selettore di data (SCHED-R10). *Verifica: gli slot configurati appaiono; il click porta alla config giusta.* **Fatto 2026-08-02:** pagina `/admin/scrapers/calendar`, sorella dell'editor degli orari nella sidebar. **Sola lettura di proposito**: questa pagina risponde a *"cosa succederà oggi"*, e il posto per cambiarlo è l'editor — due pagine che modificano gli stessi slot finirebbero prima o poi per non essere d'accordo su chi ha vinto. Il blocco è **largo quanto la run dura di solito**, così uno scraper pesante *sembra* pesante, con un minimo visibile perché anche una run da dieci secondi deve restare cliccabile; durata sconosciuta → larghezza minima e la scritta che lo dichiara, invece di far finta. Gli scraper sospesi ci sono, tratteggiati e con l'etichetta. Il click porta alla config di quello scraper. Riusa `SourceTag`, quindi l'icona è la stessa di ogni altra pagina. *Verificato: `svelte-check` 0 errori, i18n consistente.*
- [x] **10.F13 — Config plugin-declared degli scraper** (~1h): nella pagina admin dello scraper, **form dinamico** (componente di 7.F1) per le chiavi dichiarate dal plugin nel suo schema — es. le soglie degli adjustments di Dragon Store — accanto ai parametri riservati di 4.F2 ([plugin-configuration](../../docs/3-features/admin/plugin-configuration.md)). **Dipende da `10.B22`** (annotato il 2026-08-02): finché uno scraper non dichiara uno schema, questo form non ha nulla da rendere. *Verifica: soglia Dragon Store cambiata da UI → adjustments ricalcolati di conseguenza.* **Fatto 2026-08-02:** sezione *Settings from this scraper* su `/admin/scrapers/[id]`, resa dallo stesso `DynamicConfigForm` dei notifier — la pagina non impara **nessun nome di campo**, quindi uno scraper che domani dichiara un'impostazione in più compare qui senza toccare il frontend. Uno scraper che non dichiara niente **lo dice** invece di far sparire la sezione, che lascerebbe l'admin a chiedersi se manchi qualcosa. **Il vincolo di cortesia è scritto nel campo** (decisione di Simone sul termine): la didascalia di `politeness_delay_ms` dichiara che il `Crawl-delay` del sito è lo **hard minimum** e che un valore più piccolo *viene alzato*, non applicato com'è scritto — un rialzo silenzioso è indistinguibile da un campo che non funziona. Le etichette dei campi dichiarati vivono nell'i18n del plugin (`dragon_store.cfg.*`) e il prefisso è stato aggiunto ai `dynamicPrefixes` del gate, per la stessa ragione di `admin.scrapers.fields.`: quei nomi li sceglie uno schema Python e nessuna scansione dei `.svelte` può vederli.
- [x] **10.F14 — Impostazione scadenza password** (~1h): nella pagina impostazioni di sistema (10.F6), un selettore per la **scadenza della password** tra le opzioni fisse — Mai (default), 1 mese, 3 mesi, 6 mesi, 1 anno (10.B19). *Verifica: l'opzione scelta persiste e si riflette sull'enforcement al login.*
- [x] **10.F15 — Pannello statistiche cumulative dello scraper** (~1h): nella sottopagina admin dello scraper (`/admin/scrapers/[id]`, dove vivono già i parametri riservati di 4.F2 e lo svuota-cache di 4.F5), i quattro gruppi di `10.B20` — attività, traffico, salute verso il sito, resa — **numeri, non grafici**: un cumulativo graficato è una retta e non dice niente. Tre vincoli, ognuno per un modo concreto di leggere male questi numeri: il **`since` in chiaro** accanto al titolo del blocco, perché senza data di inizio un totale non significa nulla; **`consecutive_failures` in evidenza** rispetto a `runs_failed`, perché la domanda davanti a una pagina di monitoraggio è "sta fallendo adesso", non "quante volte in tutto"; e **`cache_hits` per prodotto va spiegato dove appare** (una sola cache hit HTTP serve fino a 50 prodotti di una categoria: significa "il dato veniva da una pagina in cache", non "una richiesta risparmiata"). Più il bottone di azzeramento di `10.B21`, con conferma che dichiara che non c'è storico. *Verifica: i numeri corrispondono alla riga di `scraper_stats`; nessun contatore in pagina senza la sua didascalia.* **Fatto 2026-08-02:** [`LifetimePanel.svelte`](../../src/frontend/src/lib/components/LifetimePanel.svelte) sotto le impostazioni di `/admin/scrapers/[id]` — sotto, perché le impostazioni sono ciò che si viene a cambiare e questo è ciò che si viene a controllare. I tre vincoli del MVP sono in pagina e ognuno ha la sua ragione scritta accanto: il **`since` di fianco al titolo** e non in nota, perché un totale senza data d'inizio non si interpreta affatto; la **striscia di fallimenti consecutivi in un riquadro sopra i quattro gruppi**, rossa quando non è zero, perché *«sta fallendo adesso»* è la domanda che porta qui e `runs_failed` risponde a un'altra; e la **didascalia sulle cache hit**, che sono *pagine* e non prodotti — una sola pagina di categoria ne porta decine, e letta come «richieste risparmiate» sottostimerebbe il risparmio di un fattore cinquanta. Numeri e non grafici, come previsto: un cumulativo graficato è una retta. Il bottone di azzeramento (`10.B21`) usa il `confirmDialog` di 10.T2 e la conferma dichiara che **non c'è storico**.

### Decisioni prese da Simone il 2026-08-02, secondo giro di domande

- **I titoli Markdown restano fuori** (chiude l'aperta di `10.F9`). La allow-list non cambia: un `## Sezione` continua ad appiattirsi, e l'**aiuto sotto la textarea lo dice**, così chi scrive lo sa prima invece di scoprirlo nell'anteprima. Zero codice nuovo nel renderer, e nessuna superficie in più da sanificare.
- **Il digest resta una pagina** (chiude l'aperta di `10.F19`). Il popup è per i messaggi. Una tabella con tre carrelli e venti prodotti ha bisogno di larghezza: in un dialogo centrato le colonne prezzo-prima/prezzo-dopo si stringono fino a diventare scroll dentro scroll.
- **La mail con la password è un template, ma con i placeholder obbligatori** (per `10.B16`/`10.B24`). L'admin può riscriverne il testo, e la validazione **rifiuta un override che non contiene `{password}`**: personalizzabile senza poter spedire per sbaglio una credenziale senza la credenziale dentro. È l'unico messaggio la cui rottura chiude fuori una persona, quindi è l'unico che si porta dietro una regola sua.
- **Lo scrape manuale registra tutto** (per `10.B20`): non solo i contatori di sempre ma anche una **riga di run marcata come manuale**, con un filtro pianificate/manuali/tutte nella pagina Run e le pianificate come vista di default. Chi guarda deve poter vedere *tutto* il lavoro fatto verso il sito, non solo la parte pianificata.
- **La config dichiarata dallo scraper è solo admin, e la più larga possibile** (per `10.B22`): oltre alle soglie degli adjustments anche i parametri di cortesia — attesa fra le richieste, tentativi — che oggi sono cablati. Nessun livello per-utente: *«voglio che solo l'admin possa configurare cose»*.
  > ⚠ **Assunzione mia, da smentire se sbaglio:** la cortesia resta configurabile **verso l'alto ma non sotto il `Crawl-delay` che il sito dichiara** nel suo robots.txt. La libertà chiesta è massima *dentro* l'unica garanzia che il progetto ha preso fin dalla fase 3, e un form che permette di scendere sotto quella soglia è il modo più rapido per far bandire l'installazione da un negozio — con la particolarità che il danno lo scopre chi usa il sistema, non chi ha mosso lo slider. Se la vuoi senza vincolo, si toglie in una riga.
  >
  > **Come si chiama e dove è scritto** (deciso da Simone il 2026-08-02): il termine è **hard minimum** — non "floor", non "pavimento", che in un form non dicono a chi legge né *quanto vale* né *chi lo impone*. Il vincolo **non vive solo nel codice**: il campo della cortesia porta la sua didascalia, che dichiara il valore effettivo e la sua provenienza — *«Minimum wait between requests. This site declares a `Crawl-delay` of Ns in its robots.txt: that is the hard minimum, and a lower value is raised to it.»* Un valore rialzato in silenzio è indistinguibile da un campo che non funziona, quindi il rialzo si dice **prima** (nella didascalia) e **dopo** (nel valore che il form rilegge dopo il salvataggio).
- **Le serie dei trend (`10.B6b`, `10.F5`) vanno in coda**, penultime prima di `10.D1`: la scelta fra serie per-run dentro la retention e aggregato giornaliero che le sopravvive va ragionata con calma, e la seconda costa una tabella nuova, quindi un altro `down -v`.

### Giro di modifiche del 2026-08-02, chiesto da Simone dopo la prova nel browser

Dieci punti nati guardando la fase girare davvero. Tre sono di presentazione e uno è un fastidio operativo; gli altri sei cambiano **chi è un utente e come entra**, ed è la parte che costa una ricreazione del database — `username` passa da `String(64)` a 254 perché un indirizzo email arriva fin lì, e `create_all` non altera una tabella che esiste già.

**Due decisioni prese da Simone rispondendo:** se il canale email non è configurato o è spento, creazione e reset **si rifiutano** invece di procedere (nessun account nasce inutilizzabile, nessuno resta chiuso fuori); un account nuovo ha le notifiche email **accese**, perché l'indirizzo ora è l'username e riceverle è il motivo per cui l'account esiste.

**Una decisione presa da me, di sicurezza:** la mail con la password generata **non passa dalla pipeline delle notifiche** ma è un invio diretto. Attraverso la pipeline la password finirebbe in chiaro dentro `alert_log`, cioè leggibile nello storico in-app e nel dettaglio della notifica. Una password generata deve esistere in un posto solo, e quel posto è la mail.

- [x] **10.X3 — pgweb dichiara la propria salute** (~15m): `docker ps` mostra pgweb come `Up` e basta, mentre gli altri servizi dicono `healthy`. Healthcheck in `compose-dev.yml`. *Verifica: `docker compose ps` riporta `healthy` anche per pgweb.* **Fatto 2026-08-02:** l'immagine non ha né curl né wget, ma ha **bash, e bash parla TCP** — `exec 3<>/dev/tcp/...` apre la socket e la `printf` manda una richiesta vera. Due cose imparate: va scritto come `CMD` con `bash` esplicito, perché **`CMD-SHELL` esegue dash**, che non ha `/dev/tcp` e fallisce con un fuorviante *"Directory nonexistent"* (l'ho preso in pieno al primo tentativo); e la sonda punta a `/api/connection` invece che a `/api/info`, perché il secondo risponde dai metadati di build di pgweb e continuerebbe a dire 200 con il database irraggiungibile — cioè l'unico guasto per cui questo controllo esiste.
- [x] **10.F18 — Le notifiche lette si spengono** (~15m): nello storico, una notifica letta va in grigio; solo le non lette restano in evidenza. *Verifica: il colpo d'occhio distingue letto da non letto senza leggere il pallino.* **Fatto 2026-08-02:** la riga letta passa a `text-slate-400`, e con lei il badge viola degli annunci, che smette di gridare una volta che il messaggio è stato letto. Attenuata e non nascosta: resta storico dell'utente, e un cambio di colore dice *visto* senza dire *sparito*. Il pallino non-letto resta, ma ora non è più solo lui a portare l'informazione — otto pixel sono un argomento debole per una lista lunga.
- [x] **10.T2 — Un solo modello di popup** (~1h): componente `Modal` unico (overlay, chiusura con Esc e col click fuori, focus trap, titolo, azioni) da cui derivano i popup specifici; migrati i **quattro `confirm()` nativi** (utenti, messaggi, alert, carrelli) e i **tre overlay fatti a mano** (log, schedule, catalogo). L'obiettivo dichiarato da Simone è portare più codice possibile nel parent. *Verifica: nessun `confirm()` nativo residuo; ogni popup si chiude allo stesso modo.* **Fatto 2026-08-02:** [`Modal.svelte`](../../src/frontend/src/lib/components/Modal.svelte) possiede **tutto** il comportamento — overlay, Esc, click fuori, blocco dello scroll della pagina sotto, focus che entra all'apertura e **torna da dove veniva** alla chiusura, trappola del Tab, aria. Un derivato porta contenuto e pulsanti e eredita il resto.

  Per i quattro `confirm()` nativi la traduzione diretta sarebbe stata quattro copie della stessa macchinetta a stati (apri, ricorda l'azione in sospeso, eseguila nella callback). Invece la macchinetta vive una volta sola in [`stores/confirm.ts`](../../src/frontend/src/lib/stores/confirm.ts) e il chiamante resta di **una riga**: `if (!(await confirmDialog({...}))) return;`. Chiudere senza rispondere vale **no**, che è l'unica lettura sicura. `<ConfirmHost>` è montato una volta nella shell accanto al Toaster.

  Lo schedule e il catalogo **non** usano `confirmDialog` ma il `Modal` nudo, e la ragione è istruttiva: al primo serve tenere il pulsante disabilitato mentre il salvataggio è in volo, al secondo mostrare il prodotto con la sua immagine — cose che una conferma-promessa non sa fare, perché una volta aperta non ha più una maniglia sul popup. È esattamente il caso che rende utile avere una **base** e non solo una scorciatoia. Non è rimasto nessun `confirm()` nativo, e il gestore di Esc scritto a mano nel catalogo è stato cancellato: ora è del componente. **Scelta contro `<dialog>` nativo**: il suo focus trap e il top-layer sono migliori dei miei, ma il backdrop non si può temare come il resto dell'app e il supporto Safari è arrivato tardi abbastanza da imporre comunque un fallback — una implementazione batte una più il suo fallback.
- [x] **10.F19 — NotifyViewer: la notifica si legge al centro** (~1h): derivato di `Modal`, con l'icona della lettera, che rende il messaggio al centro dello schermo. **Unifica utente e admin**, che oggi fanno due cose diverse: l'utente naviga a una pagina, l'admin espande la riga. *Verifica: dalle due pagine il click apre lo stesso popup con lo stesso render.* **Fatto 2026-08-02:** [`NotifyViewer`](../../src/frontend/src/lib/components/NotifyViewer.svelte) possiede *com'è fatto un messaggio* — la lettera, la data, il corpo reso, e uno slot per ciò che sta sotto — e niente di come si comporta un popup, che eredita tutto da `Modal`. Lato admin il corpo passa dall'**endpoint di anteprima**, cioè dal renderer che ha prodotto quello consegnato: l'admin rilegge il messaggio *com'è arrivato*, non una seconda approssimazione. 

  **Perimetro, e va detto perché è una mia lettura:** nel popup entrano i **messaggi testuali** — annunci e messaggi di sistema — che sono la cosa che le due pagine hanno in comune. Un **digest** continua ad aprire la sua pagina: è una tabella di carrelli, prodotti e prezzi, e un dialogo centrato la comprimerebbe invece di presentarla. Se intendevi *tutte* le notifiche nel popup, digest compresi, è mezz'ora in più. 

  Nella lista la riga è un `<button>` vero per un messaggio e un `<a>` vero per un digest, con **un solo corpo condiviso** via snippet: `<svelte:element>` sarebbe stato più corto e avrebbe nascosto a Svelte — e a uno screen reader — quale dei due è in quel momento, cosa che `svelte-check` ha segnalato subito. Le pagine di dettaglio restano raggiungibili come link diretti, e usano gli stessi due componenti, quindi non possono divergere sul contenuto.
- [x] **10.X2 — Il secondo pacchetto DDL: l'username diventa un indirizzo** (~30m): `users.username` a `String(254)`, nuova `users.contact_email` nullable. **Unica ricreazione del giro** — tutto il resto (validazione, generazione password, invio) non costa schema. *Verifica: `down -v`, lo stack riparte, il bootstrap crea l'admin.* **Fatto 2026-08-02:** 254 è il limite del *path* in RFC 5321, e il vecchio `String(64)` avrebbe rifiutato in silenzio indirizzi aziendali del tutto ordinari — un troncamento su una colonna `unique` è il genere di guasto che si scopre dal cliente. `contact_email` è **nullable e riservata all'admin di bootstrap**, l'unico account che esiste prima che qualcuno possa digitare una mail: per tutti gli altri resta NULL e l'indirizzo è l'username, così non ci sono mai due campi che possono litigare su dove vada la posta di una persona. Stesso metodo di `10.X1`: la colonna atterra **prima** della logica che la legge, perché `create_all` non altera una tabella esistente e una seconda ricreazione costerebbe un altro azzeramento dei contatori.
- [x] **10.B23 — L'username è una mail** (~1h): validazione via regex sia in creazione sia in modifica, normalizzazione a minuscolo in scrittura e confronto case-insensitive in lettura. **Una sola eccezione in tutto il server**: l'admin di bootstrap, che è anche l'unico account il cui username non è un indirizzo — e la regola si autosostiene, perché ogni altra creazione richiede una mail. *Verifica: `Mario.Rossi@X.IT` entra come `mario.rossi@x.it`; `pippo` viene rifiutato dall'API anche aggirando la UI.* **Fatto 2026-08-02:** [`src/core/identity.py`](../../src/core/identity.py), tre cose e basta — il limite di 254, `normalize_username()` e `is_email()`. Tre scelte dichiarate nel codice. **La normalizzazione è in scrittura, non in lettura**: se ogni username è scritto minuscolo, il login resta un `==` su una colonna unica e indicizzata, mentre un `lower()` nella query avrebbe reso l'indice inutile a ogni tentativo di accesso — e il login è l'endpoint che chiunque può battere senza autenticarsi. **La regex non è RFC 5322**: quella grammatica ammette local part fra virgolette, commenti e domain literal, cose che nessuno digita in un form e che poi ogni strumento a valle tratta a modo suo; questa accetta la forma che le persone usano davvero e rifiuta il resto. **La validazione sta nel modello Pydantic e non nel router**, così vale per ogni via d'ingresso e tutto ciò che sta a valle — il controllo di unicità compreso — confronta già la forma memorizzata. L'admin di bootstrap è **normalizzato ma non validato**: esiste prima che qualcuno possa digitare un indirizzo, ed essere l'unico username che non è una mail è precisamente il modo in cui il resto del sistema lo riconosce. 3 test nuovi; **e ne ha toccati 154 esistenti**, perché `alice` era l'username di prova di mezza suite — il vecchio contratto era inciso lì dentro, come già per ADMSG-R1b.
- [x] **10.B24 — La password la genera il sistema e la spedisce** (~1h): alla creazione e al reset, password generata, **inviata direttamente via SMTP** (mai in `alert_log`), cambio forzato al primo accesso, sessioni invalidate. Rifiuto con errore leggibile se il canale email non è utilizzabile. L'invio **ignora la preferenza dell'utente** sulle notifiche email: è una credenziale, non un avviso. *Verifica: creazione senza campo password; reset → l'utente è sloggato e la vecchia password non entra più; canale spento → 422 e nessun account creato.* **Fatto 2026-08-02:** [`src/core/direct_mail.py`](../../src/core/direct_mail.py) (allora `credentials.py`, rinominato da `10.B26`) — l'invio diretto, i due testi di default e il controllo del canale; `generate_password()` in [`security.py`](../../src/core/security.py). Scelte che vale la pena aver scritto. **L'alfabeto della password non ha `0`/`O` né `1`/`l`/`I`**: viene letta da una mail e digitata a mano una volta, e un carattere che non si distingue trasforma una credenziale funzionante in una richiesta di assistenza; costa ~0,3 bit per carattere, che a 16 caratteri ci si può permettere. **La mail parte prima che si scriva alcunché** — sia in creazione sia in reset: così un rifiuto non lascia dietro un account inutilizzabile o una password che nessuno leggerà, e il reset fallito lascia intatta quella vecchia. **Due codici distinti e non uno**: `422 email_channel_unavailable` è una precondizione che l'admin può sistemare da solo nella pagina Notifiers, `502 password_email_failed` è il canale che ha accettato e poi è caduto — l'admin non ha sbagliato niente. **La password non torna mai nella risposta**, e c'è un test che lo asserisce: esiste nella casella di posta e nell'hash, non in un terzo posto. I template vivono già nella forma che il catalogo di `10.B16` si aspetta, con `{password}` fra i `required`, così quel MVP potrà **spostarli** invece di copiarli — e un controllo finale sul testo reso rifiuta comunque un corpo che non porta la password dentro. **Il costo nella suite è stato una fixture, non venti file**: `mailed_passwords` in [`conftest.py`](../../tests/conftest.py) fissa il generatore su `temp-pass-123` (la stringa che i test usavano già, ecco perché quasi nessuno è cambiato) e registra l'invio invece di eseguirlo; i tre test che vogliono la cosa vera si marcano `real_direct_mail` e non vengono toccati dalla fixture. 3 test nuovi.
- [x] **10.B25 — L'indirizzo viene dall'account** (~1h): il notifier email non dichiara più `to_address`; il core inietta l'indirizzo effettivo (`contact_email` se c'è, altrimenti l'username). La config utente del canale si riduce all'interruttore, acceso di default. *Verifica: un utente nuovo riceve la mail senza aver configurato nulla; spegnendo il canale gli annunci arrivano solo in-app.* **Fatto 2026-08-02, e portato avanti a 10.B24** perché quest'ultimo deve spedire una password a un account appena creato: senza la convenzione qui sotto avrebbe dovuto inventarsi un modo suo di dire dove va la mail, e poi cambiarlo. La chiave `account_email` è dichiarata in [`contracts.py`](../../src/core/contracts.py) e iniettata da `merged_config()` **per ultima**, sopra admin e utente: non è una preferenza ma un fatto sull'account, e un valore rimasto in una config salvata non deve poter dirottare la posta di qualcuno. Lo schema utente dell'email diventa **vuoto** — resta l'interruttore, che vive sulla riga e non nello schema. Tre conseguenze, tutte volute. **(1)** Il test admin del canale non ha più il campo destinatario: va all'indirizzo dell'admin stesso, che è un controllo migliore della config del server rispetto a un indirizzo digitato per l'occasione, perché è quello che il sistema userà davvero (`send_test_with_config` è sparito con lui). **(2)** Nel profilo il form del canale non si disegna quando lo schema è vuoto: un form vuoto con sotto un pulsante Salva invita a un clic che non fa niente. **(3)** L'interruttore acceso è scritto **come riga alla creazione dell'account**, non come nuovo default globale — un default avrebbe riacceso il canale anche a chi lo aveva spento apposta. 2 test riscritti (uno verifica che il `To` della mail sia l'username senza che nessuno abbia configurato un recapito), documentazione del plugin email e dell'esempio in `config-field` aggiornate.
- [x] **10.F16 — Creazione utente: una mail, nessuna password** (~1h): il campo password sparisce dalla pagina utenti, l'username è un campo email validato in pagina (il backend resta l'ultima linea di difesa), e il reset dice cosa sta per succedere. *Verifica: un username non-mail è segnalato prima dell'invio.* **Fatto 2026-08-02:** campo `type="email"` con controllo in pagina, generatore e campo password rimossi. **Il controllo del browser è deliberatamente più permissivo di quello del server**, e non è una svista: il suo mestiere è intercettare un refuso prima del giro di rete, e sbagliando dal lato largo l'unica cosa che può fare è lasciar passare qualcosa che il server rifiuta — mai il contrario. Ricopiare qui la regex di `identity.py` sarebbe stata la stessa regola in due linguaggi, cioè esattamente la trappola che `9.F8` ha dovuto disfare. **L'assenza del campo password è dichiarata, non silenziosa**: una riga sotto il form dice che la genera il server e che l'admin non la vedrà — chi prima la digitava lì deve saperlo, non dedurlo. La conferma del reset ora **nomina tutte e tre le conseguenze** (nuova password, spedita, sessioni chiuse), perché nessuna delle tre si vede dal bottone. E la pagina spiega i due errori nuovi invece di dire *"non è stato possibile"*: canale email spento manda a Notifiers, invio fallito dice che **non è cambiato niente**.
- [x] **10.F17 — Profilo: l'indirizzo dell'admin, l'interruttore per tutti gli altri** (~1h): l'admin di bootstrap imposta il proprio `contact_email` (è l'unico che non ce l'ha nell'username); ogni altro utente vede l'indirizzo in sola lettura e può solo accendere o spegnere le notifiche email. *Verifica: un utente normale non ha modo di cambiare l'indirizzo.* **Fatto 2026-08-02** — nuovo requisito **PROF-R12** in [profile-and-notifiers](../3-features/user/profile-and-notifiers.md). `GET /api/me` porta due campi nuovi: `notification_email` (l'indirizzo effettivo, già risolto lato server, così la pagina non rifà il `contact_email or username`) e `email_editable`. **Chi può modificare non è il ruolo ma la forma dell'username**: `not is_email(username)` è vero per il solo admin di bootstrap, e la regola si autosostiene perché ogni altro account nasce da un'API che pretende un indirizzo — nessun flag da mantenere allineato. Per tutti gli altri il campo non è disabilitato ma **assente**, con una riga che dice perché: un input grigio suggerisce *"forse più tardi"* su una cosa che non succederà mai. Lato server il rifiuto è un `403 address_not_editable` e non solo un campo nascosto. 2 test nuovi.

### Secondo giro dopo la prova nel browser, chiesto da Simone il 2026-08-02

Quattro punti, e sono tutti la stessa osservazione da angoli diversi: **una funzione che non fa più niente va tolta, non lasciata lì disabilitata**. Il test dal profilo e il reset password su un account in cancellazione erano entrambi bottoni che rispondevano a domande che nessuno ha più.

**Una decisione presa da me, e va dichiarata perché estende quanto chiesto:** Simone ha chiesto il bypass della preferenza email per le notifiche di **cancellazione**; l'ho applicato anche alla **disabilitazione**, che è lo stesso caso con lo stesso argomento — la copia in-app di *"il tuo account è disabilitato"* si legge solo accedendo, cioè facendo la cosa che quel messaggio dice essere diventata impossibile. Se l'intenzione era il solo perimetro cancellazione, tornare indietro è una riga.

- [x] **10.X4 — Via l'invio di prova dal profilo** (~30m): sparisce il bottone *Send test* dalla sezione canali del profilo, e con lui l'endpoint `POST /api/notifiers/{id}/test`, il client TypeScript e le tre stringhe. Resta la prova lato admin, che è quella che serve mentre si configura SMTP. *Verifica: l'endpoint risponde 404 anche chiamato a mano; la pagina admin continua a spedire la prova.* **Fatto 2026-08-02:** cancellato, non nascosto — una rotta lasciata in piedi e solo scollegata dalla UI torna viva alla prima persona che apre la pagina OpenAPI, ed è un invio di mail autenticabile da qualunque utente. La ragione per cui non serve più è già scritta nella storia della fase: `10.B25` ha tolto il campo destinatario e `10.B23` ha reso l'indirizzo l'account stesso, quindi **il recapito si è già dimostrato funzionante portando la password con cui quella persona è entrata**. Quel che restava era una sonda sulla configurazione SMTP del server esposta in una pagina il cui proprietario non può metterci mano. Il test che copriva l'endpoint utente non è stato buttato ma **spostato sull'admin** — la stessa asserzione (il `To` viene dall'account, non da un campo) su quello che è rimasto, più uno nuovo che pretende il 404.
- [x] **10.B26 — Le notizie sull'account arrivano comunque** (~1h): disabilitazione, marcatura per cancellazione e cancellazione definitiva **ignorano la preferenza sulle notifiche email**; nuova voce `user.deleted` nel catalogo, e il testo della marcatura dice esplicitamente che l'account è in lista per essere rimosso in data `{deletion_due_date}`. *Verifica: utente con email spenta → la mail arriva lo stesso; utente con email accesa → arriva una sola volta.* **Fatto 2026-08-02:** [`src/core/credentials.py`](../../src/core/credentials.py) è diventato [`direct_mail.py`](../../src/core/direct_mail.py), perché adesso ospita **due** famiglie di messaggi che scavalcano la pipeline per due ragioni diverse — le credenziali perché finirebbero in chiaro nello storico, queste perché sono precisamente la notizia che non puoi venire a leggere in-app. Tenere il vecchio nome sarebbe stato falso sulla metà nuova.

  **Due invii, e il motivo per cui non sono due copie della stessa mail**: `send_account_notice()` scrive la copia in-app sui canali normali **meno email**, poi manda la mail diretta. Togliere email dal primo passaggio è l'unica cosa che evita il doppione, e c'è un test che lo pretende guardando le righe di `alert_delivery` (`{"in_app"}` e nient'altro). Gli altri canali continuano a seguire la preferenza: è **email** il posto dove l'account è raggiungibile da `10.B23`, non un Telegram che qualcuno collegherà.

  **La mail di cancellazione parte dopo il commit**, ed è per questo che nome e indirizzo si leggono dalla riga *prima* di distruggerla: annunciarla prima sarebbe una bugia nel caso — previsto e già gestito — in cui un plugin rifiuti e l'account sopravviva. Il fallimento dell'invio è best-effort e finisce nel log: a quel punto non c'è più niente da annullare.

  **Corretto per strada un dettaglio che era sbagliato da `10.B24`**: `username` e `address` viaggiavano come un solo valore, così l'admin di bootstrap — l'unico account in cui i due differiscono — si sarebbe visto scrivere *"Username: la-tua-mail@..."* con un indirizzo con cui non entra. Ora sono due parametri distinti. Nella suite il costo è stato di nuovo una fixture (`mailed_notices`), per la ragione pratica che senza stub ogni test che disabilita un utente chiede al plugin email di raggiungere un SMTP che non esiste, e la paga in timeout.
- [x] **10.B27 — Cancellare adesso, rinunciando alla scadenza** (~45m): `DELETE /api/admin/users/{id}/purge` esegue subito, su un account **già marcato**, quello che il job notturno avrebbe fatto alla scadenza. Nuovo requisito **USR-R9b**. *Verifica: su un account non marcato → `409`; su uno marcato → `204` e la riga sparisce; sul proprio → `403`.* **Fatto 2026-08-02:** il corpo della cancellazione è stato **estratto**, non riscritto: `purge_user()` in [`user_purge.py`](../../src/core/user_purge.py) è ora la funzione che entrambi gli inneschi chiamano, e `purge_due_users()` è diventato il ciclo che la invoca sugli scaduti. Scriverne una seconda versione nel router avrebbe significato due risposte alla domanda *"e i dati dei plugin?"*, che è esattamente il tipo di divergenza che `10.B20` ha dovuto rimediare sui contatori.

  **Solo su un account già marcato**, e non è prudenza generica: la finestra reversibile è l'intera ragione per cui esiste `10.B3`, e un unico bottone che distrugge un account la annullerebbe per tutti — non solo per chi lo preme apposta. Due passi restano due passi; il secondo dice *non quella data, adesso*.
- [x] **10.F20 — Sulla riga in cancellazione, l'azione che ha senso** (~30m): *Reset password* sparisce dalle righe marcate e al suo posto compare *Delete permanently*, con una conferma che nomina la data a cui si sta rinunciando, cosa sparisce e il fatto che la persona viene avvisata. *Verifica: la conferma dice tutte e tre le cose; annullare non cancella niente.* **Fatto 2026-08-02:** il reset su un account in cancellazione era un bottone che **non poteva servire a niente** — la mail sarebbe partita, la nuova password sarebbe stata valida, e il login l'avrebbe rifiutata comunque perché il gate della cancellazione viene prima. Il posto era occupato dall'unica azione che quella riga non aveva. La conferma non chiede *"sei sicuro?"*: l'admin è sicuro, quello che può non sapere è cosa succede esattamente, quindi il testo lo elenca.

### Rifiniture sulle conferme, chieste da Simone il 2026-08-02 (terzo giro)

- [x] **10.F21 — Nelle conferme, il dato variabile è in grassetto** (~30m): nome utente e data escono in evidenza dentro il testo del popup. *Verifica: la conferma di cancellazione mostra account e data in grassetto; il resto della frase no.* **Fatto 2026-08-02:** `ConfirmRequest` guadagna `highlight?: string[]` e `<ConfirmHost>` spezza il messaggio su quelle sottostringhe. **Passati come dati, non come markup**, e le due alternative erano entrambe peggiori: il Markdown nel messaggio vorrebbe un renderer nel client, quando l'app rende Markdown lato server proprio perché una regola sola non diventi due (`9.F8`); i tag nella traduzione vorrebbero `{@html}` su una stringa che contiene un username, cioè testo non fidato dentro il DOM per rendere una parola più pesante. La ricerca è una **scansione e non una regex**, perché gli aghi sono dati: una data formattata porta punti e barre che una regex leggerebbe come sintassi. Un dettaglio che si vede solo provandolo: il blocco `{#each}` è scritto **su una riga sola** con `prettier-ignore`, perché il paragrafo ha `whitespace-pre-line` e l'indentazione del template inserirebbe un a capo fra un pezzo di testo e l'altro.
- [x] **10.F22 — Abilita e disabilita chiedono conferma** (~20m): erano le uniche due azioni della riga che partivano al click. *Verifica: entrambe aprono un popup; annullare non cambia lo stato.* **Fatto 2026-08-02:** in entrambe le direzioni, perché nessuna delle due è solo un interruttore — disabilitare chiude ogni sessione aperta e manda una mail, riabilitare rimette dentro qualcuno che era stato chiuso fuori apposta. Il testo dice quale delle due cose sta per succedere invece di chiedere *"sei sicuro?"*, e i due bottoni stanno per giunta dove sulle righe marcate c'è una cancellazione: un dito che sbaglia riga trova comunque una domanda.

### Quarto giro, chiesto da Simone il 2026-08-02

- [x] **10.F23 — Save si accende solo se qualcosa è cambiato** (~1h): vale per **tutti** i form con un tasto Save, non solo per la config del notifier da cui è nata l'osservazione. *Verifica: aprire una pagina di configurazione e premere Save senza toccare niente non è possibile; cambiare un campo lo riaccende.* **Fatto 2026-08-02** — nuova regola vincolante **FE-19** in [frontend rules](../../docs/developer-rules/frontend/rules.md). L'osservazione di Simone era precisa: *"il tasto enable/disable funziona a prescindere da save"*, cioè su quella scheda convivono un controllo che agisce subito e uno che agisce al click — e se il secondo è sempre acceso anche quando non ha niente da scrivere, smette di significare qualcosa. Il confronto vive in un posto solo, [`$lib/forms.ts`](../../src/frontend/src/lib/forms.ts), perché la variante scritta a mano per pagina è quella che dimentica il campo aggiunto il mese dopo. Coperti: il form dinamico (notifier admin e utente, config dichiarata dello scraper), impostazioni di sistema, feature flag, parametri riservati dello scraper, editor dei messaggi di sistema, indirizzo nel profilo, rinomina carrello e soglia carrello. **Il caso interessante sono i segreti**: la casella parte vuota e vuoto significa *"tieni quello salvato"*, quindi un form intonso con un segreto già impostato è davvero pulito — è la stessa regola che usa già l'invio (`CFG-R3`), non una seconda. Dopo il salvataggio la baseline si riallinea in modo **ottimistico**: le pagine che possiedono questi form gestiscono i propri errori con un toast e non rilanciano, quindi l'alternativa sarebbe un form sporco per sempre dopo un singolo errore del server; quello che l'admin aveva scritto resta comunque nelle caselle e toccare un campo riarma Save.
- [x] **10.F24 — La pagina messaggi dice se la mail non parte** (~30m): se il canale email è spento o non configurato, un banner avvisa che il messaggio comparirà **solo in-app**. *Verifica: spegnere il canale → il banner compare sopra il compositore, con un link a Notifiers.* **Fatto 2026-08-02:** due cause distinte e non una — l'interruttore globale spento e la config SMTP incompleta portano allo stesso risultato ma si risolvono in due modi diversi, quindi il testo lo dice. Il banner **non blocca l'invio**: la copia in-app esiste sempre (ADMSG-R2) e un annuncio letto solo in app è comunque un annuncio consegnato; la sua funzione è togliere all'admin la sorpresa a cose fatte. Se la chiamata che lo alimenta fallisce il banner tace: comporre un messaggio non deve dipendere dal riuscire a descrivere il canale.
- [x] **10.F25 — Il badge del canale dice se sta consegnando** (~20m, dalla domanda di Simone *"cosa significa Available?"*): tre stati — **Active**, **Switched off**, **Not configured** — al posto dei due precedenti. **Fatto 2026-08-02:** la domanda era il bug. `Available` era legato al solo `admin_config_complete`, cioè *"la config di sistema è completa"*, e non guardava l'interruttore: un canale spento con il bottone *Enable* accanto continuava a dichiararsi disponibile. Il badge ora risponde alla domanda che l'admin sta davvero facendo — *sta uscendo qualcosa da qui?* — e il vecchio significato non si perde, perché sia *Active* sia *Switched off* implicano una config completa.

### In coda alla fase — da discutere prima di stimare (nessuna implementazione senza discussione)

> ⛔ **Non implementare nulla di questo blocco prima di averlo discusso e deciso con Simone.** È un'idea annotata il 2026-07-27, non un MVP: manca la scelta di fondo, e sceglierla male costa più che rimandarla. **Spostato in fondo il 2026-08-02, su decisione di Simone: si tratta per ultimo**, dopo che tutto il resto della fase è chiuso. Ha anche una ragione tecnica: la domanda architetturale che si porta dietro — *è un notifier con destinatario admin, o un sistema parallelo?* — si risponde meglio con la pipeline dei messaggi admin (10.B12–10.B17) già costruita e davanti agli occhi.

- [ ] **10.D1 — Notifiche per l'admin sui log** *(da discutere)*: oggi i canali di notifica (fase 7) servono **solo l'utente** e solo per eventi di catalogo/carrello; l'admin non riceve nulla, e per accorgersi di un problema deve aprire la pagina dei log e guardare. L'idea è una sezione di notifiche **propria dell'admin**, dove sottoscriversi a ciò che il sistema logga — l'esempio concreto è "mandami una notifica quando il portale logga degli `error`". Il caso reale che la motiva è il blocco di Dragon Store del 2026-07-25: gli errori c'erano nei log dal primo giorno, ma nessuno li ha visti finché Simone non è andato a leggerli.
  >
  > **La decisione aperta è la logica di attivazione**, e le due strade non sono equivalenti:
  > - **Istantanea** — si notifica appena il log viene scritto. Reattiva, ma un guasto ripetitivo genera una raffica: lo scraper che fallisce su 40 watch produrrebbe 40 notifiche identiche. Servirebbe come minimo un raggruppamento o un antiflood.
  > - **A finestre** — un engine dedicato all'admin che gira ogni tot ore, guarda i log del periodo e decide cosa è **significativo** (un errore isolato non lo è, venti errori uguali sì; un errore nuovo mai visto prima probabilmente sì). Più simile a come funzionano già gli alert utente, che sono per digest e non per evento singolo.
  >
  > Altri punti da chiarire quando se ne parla: chi riceve (tutti gli admin? il super-user di fase 9?); quali canali (riuso di quelli di fase 7 o un percorso separato); che granularità di sottoscrizione (per livello, per sorgente, per testo); e se questo debba diventare un **notifier con destinatario admin** dentro l'infrastruttura esistente invece di un sistema parallelo — che è la domanda architetturale più importante delle quattro.

## Definition of Done

- [ ] Tutte le [pagine admin](../4-capabilities/frontend/app-shell.md#pagine-admin) esistono e sono usabili.
- [ ] Le domande del [flusso di lettura admin](../3-features/admin/scraper-monitoring.md#flusso-di-lettura-tipico-delladmin) trovano risposta nella UI.
- [ ] L'admin continua a non vedere alcun **contenuto** operativo degli utenti: la dashboard espone solo aggregati e conteggi (DASH-R6).
- [ ] I contatori per scraper di `9.B6c` sono **visibili e onesti**: nessun numero senza la sua didascalia, il `since` sempre dichiarato, e il traffico dello scrape manuale contato come quello schedulato (il buco di `10.B20`). Per ogni contatore dell'inventario è stato deciso se si mostra — **anche quando la decisione è "non si mostra"**.
- [ ] [docs](../../docs/) aggiornata in inglese con la sola parte implementata in questa fase (DOC-12).

## Riferimenti

[admin-experience](../1-business/admin-experience.md) · [scraper-scheduling-and-limits](../../docs/3-features/admin/scraper-scheduling-and-limits.md)
